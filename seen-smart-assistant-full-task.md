# مهمة شاملة: نظام "مساعد سين الذكي" الكامل (Widget + Super Admin Settings)

## 🎭 الدور (Role)
تصرف كـ **Senior AI Full-Stack Engineer** و **UX/UI Expert** متخصص في:
- React (Next.js App Router) + TypeScript
- Tailwind CSS v4
- Vercel AI SDK (`ai` package)
- تصميم أنظمة Multi-tenant SaaS وواجهات RTL احترافية بالعربية

ابدأ بعرض **خطة عمل (Todo List)** بكل الملفات التي ستنشئها/تعدّلها، ثم نفّذها ملفًا تلو الآخر، واعرض بعد كل ملف ملخصًا سريعًا قبل الانتقال للتالي.

---

## 🎯 السياق العام (Context)
النظام هو **"سين"** — نظام نقاط بيع سحابي (SaaS) متعدد المستأجرين (Multi-tenant) لمحلات الخياطة. المشروع يحتوي على 3 مستويات مستخدمين:
- **Super Admin**: يدير المنصة بالكامل (كل المحلات/Tenants).
- **Admin**: صاحب المحل (Tenant).
- **Cashier**: كاشير داخل المحل.

المطلوب بناء **منظومة متكاملة** لمساعد ذكاء اصطناعي داخل النظام، تتكوّن من جزأين:

1. **Chat Widget** يظهر للمستخدمين (Admin/Cashier) في كل صفحات النظام للرد على استفساراتهم.
2. **صفحة إعدادات في لوحة السوبر أدمن** للتحكم الكامل بسلوك المساعد وتفعيله/تعطيله عبر المنصة، **بدون الحاجة لتعديل الكود أو إعادة النشر (Deployment)**.

---

## 📦 المخرجات المطلوبة (Deliverables)

| # | الملف | الوصف |
|---|---|---|
| 1 | `components/SmartAssistant.tsx` | مكوّن الـ Chat Widget (الواجهة الأمامية) |
| 2 | `app/api/chat/route.ts` | API لمحادثة المستخدم مع المساعد (Streaming) |
| 3 | `components/Layout.tsx` | دمج الـ Widget في كل الصفحات ما عدا تسجيل الدخول |
| 4 | `app/(super-admin)/assistant-settings/page.tsx` | صفحة إعدادات المساعد في السوبر أدمن |
| 5 | `components/super-admin/AssistantSettingsForm.tsx` | نموذج (Form) الإعدادات نفسه |
| 6 | `app/api/super-admin/assistant-settings/route.ts` | API لقراءة/حفظ الإعدادات (GET/PUT) — محمي بصلاحية Super Admin فقط |
| 7 | تعديل الـ Schema (Prisma أو ORM المستخدم فعليًا في المشروع) | إضافة جدول/موديل `AssistantSettings` |

> ⚠️ **ملاحظة مهمة:** الأمثلة أدناه تفترض استخدام Prisma كـ ORM. إذا كان المشروع يستخدم ORM آخر (Drizzle، TypeORM، إلخ) أو قاعدة بيانات مختلفة، عدّل الكود ليتوافق مع ما هو موجود فعليًا في المشروع بدل افتراض Prisma تلقائيًا — **افحص المشروع أولًا قبل الكتابة**.

---

## 1️⃣ نموذج البيانات (Data Model) — الأساس الذي يبني عليه كل شيء

أنشئ موديل `AssistantSettings` يخزّن إعدادات عامة على مستوى المنصة (Global Settings)، بالحقول التالية:

```prisma
model AssistantSettings {
  id                String   @id @default(cuid())
  isEnabled         Boolean  @default(true)      // تفعيل/تعطيل المساعد كليًا في كل النظام
  aiProvider        String   @default("openai")  // "openai" | "gemini"
  modelName         String   @default("gpt-4o-mini")
  apiKeyEncrypted   String?                        // مخزّن مشفّرًا، لا يُعاد إرساله كاملاً للفرونت إند أبدًا
  systemPrompt      String   @db.Text             // قابل للتعديل من لوحة السوبر أدمن
  temperature       Float    @default(0.7)
  maxTokens         Int      @default(500)
  dailyMessageLimit Int      @default(200)         // سقف الرسائل اليومي لكل Tenant (0 = بلا حد)
  updatedAt         DateTime @updatedAt
  updatedBy         String?                        // معرّف السوبر أدمن الذي حفظ آخر تعديل
}
```

**قواعد أمان إلزامية:**
- مفتاح الـ API يُخزَّن **مشفّرًا** في قاعدة البيانات (استخدم مكتبة تشفير موجودة في المشروع، أو `crypto` القياسية إن لم توجد).
- عند إرجاع الإعدادات إلى الواجهة الأمامية عبر GET، **لا تُعد مفتاح الـ API كاملاً أبدًا** — أرجع فقط آخر 4 خانات منه مع علامات إخفاء (مثل: `sk-••••••••ab12`).
- إذا أرسل السوبر أدمن الحقل فارغًا عند الحفظ، **لا تحذف** المفتاح الحالي (يعني الحقل الفارغ = "لم يتغيّر"، وليس "احذف المفتاح").

---

## 2️⃣ الـ Chat Widget — `SmartAssistant.tsx`

### الزر العائم (FAB)
- ثابت أسفل يسار الشاشة (`fixed bottom-6 left-6`) — لأن النظام RTL. أيقونة من `lucide-react`.
- **إذا كانت `isEnabled = false` في الإعدادات، لا يُعرض الزر إطلاقًا** (اجلب الحالة عبر endpoint خفيف مثل `GET /api/assistant-settings/status` يرجع فقط `{ isEnabled: boolean }` بدون أي بيانات حساسة).

### نافذة المحادثة
| القسم | المتطلبات |
|---|---|
| **Header** | "مساعد سين" + زر إغلاق (X) |
| **Messages Area** | تمرير (`overflow-y-auto`)، رسائل المستخدم بلون العلامة التجارية محاذاة يمين، رسائل المساعد بخلفية رمادية فاتحة |
| **Input Area** | حقل نص + زر إرسال (Input Group) نظيف |

### حالات إضافية يجب التعامل معها
- **Typing Indicator** أثناء انتظار الرد (3 نقاط متحركة).
- **حالة تجاوز الحد اليومي (`dailyMessageLimit`)**: عرض رسالة واضحة للمستخدم بدل محاولة إرسال فاشلة صامتة.
- **حالة تعطيل المساعد أثناء استخدامه** (Super Admin عطّله من لوحة التحكم وأنت مفتوح على المحادثة): تعامل مع خطأ 403 من الـ API بعرض رسالة مناسبة وإغلاق تلقائي للنافذة.

### التجاوب والاتجاه
- `dir="rtl"` بالكامل، Tailwind v4 فقط (بدون صيغ v3 المهجورة).
- على الجوال: نافذة المحادثة Full-screen sheet.

### الربط بـ Vercel AI SDK
- استخدم `useChat` من `ai/react`.
- Props المطلوبة:
```ts
interface SmartAssistantProps {
  userName: string;
  userRole: "Admin" | "Cashier";
  tenantId: string;
}
```
- مرّرها ضمن `body` في `useChat({ api: "/api/chat", body: { userName, userRole, tenantId } })`.

---

## 3️⃣ الواجهة الخلفية للمحادثة — `app/api/chat/route.ts`

خلافًا للنسخة الأولى، الـ System Prompt والإعدادات **لم تعد ثابتة في الكود** — يجب:

1. جلب `AssistantSettings` الحالية من قاعدة البيانات في بداية كل طلب.
2. إذا `isEnabled === false` → أرجع `403` فورًا مع رسالة واضحة، ولا تكمل الطلب للنموذج.
3. تحقّق من `dailyMessageLimit` (عدّاد رسائل يومي لكل `tenantId` — استخدم جدول بسيط أو Redis إن كان متوفرًا في المشروع؛ إن لم يوجد، اقترح أبسط حل ممكن ووثّقه في تعليق بالكود).
4. ابنِ الـ System Prompt ديناميكيًا بدمج:
   - `systemPrompt` من الإعدادات (القالب الأساسي الذي كتبه السوبر أدمن).
   - بيانات السياق: اسم المستخدم ودوره (Admin/Cashier) — لتخصيص الأسلوب حسب الدور.
5. استخدم `aiProvider`, `modelName`, `temperature`, `maxTokens` من الإعدادات المخزّنة (وليس قيمًا ثابتة في الكود) عند استدعاء `streamText`.
6. فكّ تشفير `apiKeyEncrypted` فقط في هذه البيئة الخادمية (Server-side)، ولا تسمح بتسريبه في أي استجابة أو سجل (log).

**Fallback افتراضي** (إن لم توجد إعدادات محفوظة بعد في قاعدة البيانات، أول تشغيل للنظام):
> "أنت مساعد سين الذكي، نظام نقاط بيع لمحلات الخياطة. مهمتك مساعدة صاحب المحل أو الكاشير بأسلوب ودود ومهني، الإجابة باختصار، وتوجيههم لكيفية إنشاء فواتير أو جرد المخزون."

---

## 4️⃣ صفحة إعدادات السوبر أدمن — `app/(super-admin)/assistant-settings/page.tsx`

### حماية الوصول (إلزامي)
- الصفحة و الـ API الخاصة بها يجب أن تكونا محميتين بحيث **فقط مستخدم بدور Super Admin** يستطيع الوصول إليهما (استخدم آلية الحماية/الـ Middleware الموجودة فعليًا في المشروع — لا تخترع نظام صلاحيات جديد).
- إن حاول أي مستخدم آخر الوصول لمسار الـ API مباشرة → `401/403`.

### محتوى الصفحة (عبر `AssistantSettingsForm.tsx`)

اجعل النموذج مقسّمًا لأقسام واضحة:

**أ) التفعيل العام**
- Toggle switch كبير وواضح: "تفعيل مساعد سين" (ON/OFF) — هذا يوقف/يشغّل الـ Widget في كامل المنصة فورًا.

**ب) إعدادات النموذج (AI Provider)**
- Dropdown لاختيار المزوّد: OpenAI / Gemini.
- حقل اسم النموذج (Model Name) كنص حر أو Dropdown بخيارات شائعة (`gpt-4o-mini`, `gpt-4o`, `gemini-1.5-flash`, إلخ).
- حقل مفتاح API (`type="password"`، مع زر عين لإظهاره مؤقتًا)، ويعرض دائمًا القيمة المخفية الحالية (آخر 4 خانات) كـ placeholder إن كان محفوظًا مسبقًا.
- Slider أو حقل رقمي لـ `temperature` (0 - 1).
- حقل رقمي لـ `maxTokens`.

**ج) القالب النصي (System Prompt)**
- `<textarea>` كبير قابل للتعديل الحر يعرض القيمة الحالية لـ `systemPrompt`.
- زر "استعادة الافتراضي" (Reset to Default) يعيد تعبئة الحقل بالنص الافتراضي المذكور في القسم 3 دون حفظ تلقائي (يحتاج المستخدم للضغط على "حفظ" بعدها).

**د) الحدود والاستخدام**
- حقل رقمي لـ `dailyMessageLimit` لكل Tenant (مع توضيح أن القيمة 0 تعني "بلا حد").

**هـ) اختبار حي (Live Preview) — مهم جدًا**
- صندوق دردشة مصغّر داخل صفحة الإعدادات نفسها يسمح للسوبر أدمن بتجربة المساعد **بالإعدادات الحالية في النموذج (حتى قبل الحفظ)** — أرسل هذا الاختبار عبر endpoint منفصل `POST /api/super-admin/assistant-settings/test` يستقبل الإعدادات المؤقتة من الفورم مباشرة ولا يقرأها من قاعدة البيانات، حتى يرى السوبر أدمن أثر تعديلاته فورًا قبل تأكيد الحفظ.

**و) الحفظ**
- زر "حفظ التغييرات" يرسل `PUT` إلى `app/api/super-admin/assistant-settings/route.ts`.
- عرض Toast/رسالة نجاح أو خطأ واضحة بعد الحفظ.
- عرض "آخر تحديث بواسطة [اسم] بتاريخ [كذا]" أسفل النموذج (من `updatedAt` و`updatedBy`).

### تصميم وتجربة المستخدم
- التزم بنفس نمط تصميم لوحة السوبر أدمن الحالي في المشروع (لا تبتكر نظام تصميم منفصل — افحص المكوّنات الموجودة وأعد استخدامها: بطاقات، أزرار، حقول إدخال... إلخ).
- `dir="rtl"` كاملة، متجاوبة مع الجوال.
- أي إجراء "خطير" (مثل تعطيل المساعد بالكامل أو تغيير المزوّد) يُفضّل تأكيده عبر Dialog تأكيد بسيط قبل التنفيذ.

---

## 5️⃣ الدمج النهائي — `Layout.tsx`
- استورد `SmartAssistant` وضعه بحيث يظهر فوق كل المحتوى (`z-50`+).
- مرّر بيانات المستخدم الحالي من الـ session/context الموجود في المشروع.
- استثنِ صفحة تسجيل الدخول (`/login`) عبر `usePathname()` أو ما يعادلها.
- **جديد**: استثنِ أيضًا صفحات السوبر أدمن نفسها (`/super-admin/**`) — لأن السوبر أدمن يتعامل مع صفحة الإعدادات المخصصة له، وليس الـ Widget العام الموجّه للـ Admin/Cashier.

---

## ✅ معايير القبول (Definition of Done)

**الـ Widget:**
- [ ] لا يظهر الزر العائم إطلاقًا عند `isEnabled = false`.
- [ ] الـ Streaming يعمل فعليًا (رد تدريجي).
- [ ] رسالة واضحة عند تجاوز `dailyMessageLimit`.
- [ ] رسالة واضحة + إغلاق تلقائي عند تعطيل المساعد أثناء محادثة مفتوحة.
- [ ] RTL صحيح 100% ومتجاوب على الجوال.

**صفحة إعدادات السوبر أدمن:**
- [ ] الوصول محمي بصلاحية Super Admin فقط (تحقّق فعلي، ليس فقط إخفاء بصري).
- [ ] مفتاح الـ API لا يُعرض كاملاً أبدًا في أي استجابة API.
- [ ] الحفظ يعمل ويعكس التغييرات فورًا على الـ Widget في كل المنصة دون إعادة نشر.
- [ ] الاختبار الحي (Live Preview) يستخدم القيم غير المحفوظة من الفورم مباشرة.
- [ ] زر "استعادة الافتراضي" يعمل بدون حفظ تلقائي.
- [ ] يظهر "آخر تحديث بواسطة... بتاريخ..." بشكل صحيح.

**عام:**
- [ ] لا أخطاء TypeScript.
- [ ] Tailwind v4 فقط في كل الملفات.
- [ ] الكود يفحص أدوات/مكتبات المشروع الفعلية (ORM، نظام الصلاحيات، نظام التصميم) قبل افتراض أي شيء جديد.

---

## 📝 ملاحظة للتنفيذ
نفّذ بالترتيب التالي لتفادي الاعتماديات المكسورة:
1. نموذج البيانات (Schema + Migration).
2. API الإعدادات (`super-admin/assistant-settings`).
3. صفحة وفورم الإعدادات.
4. تحديث `api/chat/route.ts` ليقرأ من الإعدادات بدل القيم الثابتة.
5. `SmartAssistant.tsx` مع التحقق من `isEnabled`.
6. الدمج في `Layout.tsx`.

بعد كل خطوة، أعطِ ملخصًا مختصرًا لما تم قبل الانتقال للتالية.
