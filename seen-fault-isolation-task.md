# مهمة: عزل الأعطال — خطأ في جزء واحد لا يوقف النظام بالكامل

## 🎭 الدور (Role)
تصرف كـ **Senior Full-Stack Engineer** متخصص في React Error Boundaries وموثوقية خوادم Express/Node. هذه مهمة **إضافة طبقات حماية (defensive/additive)** — لا تحذف أو تعيد كتابة أي معالجة أخطاء موجودة حاليًا، فبعضها جيد فعلاً كما سترى في السياق أدناه.

ابدأ بعرض خطة عمل (Todo List)، نفّذ الواجهة الأمامية أولاً (المرحلة 1، الأثر الأكبر)، ثم تحصين الخادم (المرحلة 2)، ثم التحقق (المرحلة 3).

---

## 🎯 السياق — تدقيق فعلي على الكود

### الواجهة الأمامية: المشكلة حقيقية وواضحة
يوجد بالفعل مكوّن `src/components/ErrorBoundary.tsx` **جيد الصنع** (يلتقط أخطاء React، وrejections غير معالجة، وأخطاء تحميل chunks، ويميّز أخطاء الصلاحيات عن أخطاء الشبكة). **لكن مشكلته أنه يُستخدم مرة واحدة فقط**، ملفوفًا حول كامل `<App />` في `src/main.tsx`:

```tsx
<ErrorBoundary><App /></ErrorBoundary>
```

هذا يعني: **أي خطأ في أي مكان — من أي صفحة، من أي دور مستخدم — يُسقط الشجرة بأكملها** ويعرض شاشة "حدث خطأ" كاملة تتطلب `window.location.reload()` (إعادة تحميل كامل النظام). بالضبط المشكلة التي وصفتها.

هيكل التوجيه في `src/App.tsx` (React Router) منظّم فعلاً بشكل جيد إلى مجموعات مسارات منفصلة تحت `<React.Suspense>` لكل دور: مسارات السوبر أدمن (`/admin/*` حوالي السطر 548-599)، مسارات المالك/الأدمن (حوالي السطر 642-712)، مسارات الكاشير (حوالي السطر 740-806) — **لكن لا يوجد `ErrorBoundary` واحد منها**، فقط `Suspense` (يعالج التحميل، لا يعالج الأخطاء).

مكوّن **المساعد الذكي** (يُدمج داخل `Layout.tsx` ويظهر في كل الصفحات) معروف بتاريخ أعطال حية موثّقة (انهيارات production سابقة تم تشخيصها من سجلات Vercel). بما أنه يُعرض داخل نفس شجرة الصفحة، **أي عطل فيه اليوم يُسقط الصفحة كاملة معه** بدل أن يختفي الودجت فقط.

### الخادم: الوضع أفضل مما هو متوقع، لكن ناقص طبقة أخيرة
تدقيق `server.ts` (20 مسارًا) أظهر أن **كل مسار تقريبًا محاط بـ `try/catch` فعليًا** — انضباط جيد موجود مسبقًا، لا تُعِد كتابته. الفجوة الحقيقية: **لا يوجد process.on('uncaughtException'/'unhandledRejection')، ولا يوجد middleware معالجة أخطاء عام (4 معاملات: `(err, req, res, next)`) في نهاية الملف.** يعني: أي خطأ يفلت من كل تلك الـ try/catch (كود مستقبلي ينسى try/catch، أو خطأ متزامن في middleware قبل الوصول للمسار) لا توجد شبكة أمان أخيرة تلتقطه.

---

## 1️⃣ المرحلة 1: عزل الأعطال في الواجهة (الأثر الأكبر)

### أ. أضف نوعًا "خفيفًا" (inline) للمكوّن الموجود
لا تُنشئ مكوّنًا جديدًا من الصفر — وسّع `ErrorBoundary.tsx` الحالي بخاصية اختيارية (مثلاً `variant?: 'full' | 'inline'`):
- `full` (الافتراضي الحالي، بلا تغيير): الشاشة الكاملة الدرامية — تبقى فقط على الغلاف الأعلى في `main.tsx` كشبكة أمان نهائية.
- `inline`: بطاقة صغيرة داخل مكانها في الصفحة (لا تملأ الشاشة)، برسالة مختصرة وزر "إعادة المحاولة" الذي يُعيد تعيين حالة هذا الـ boundary تحديدًا (**بدون** `window.location.reload()` — هذا هو الفرق الجوهري المطلوب).

### ب. لفّ كل مجموعة مسارات + كل مسار فردي بـ boundary خاص بها
في `App.tsx`، أضف `<ErrorBoundary variant="inline">` حول محتوى كل `<Route element={...}>` فرديًا (وليس فقط حول مجموعة الـ `<Routes>` ككل) — بحيث خطأ في صفحة "التقارير" مثلاً لا يُسقط صفحة "الطلبات" المجاورة، والمستخدم يقدر يتنقّل لصفحة أخرى تعمل بدل أن يُحبس على شاشة خطأ كاملة.

### ج. اعزل ودجت المساعد الذكي تحديدًا
في `Layout.tsx`، لفّ مكوّن المساعد الذكي (`SeenAIFab` أو ما يعادله) بـ `<ErrorBoundary variant="inline">` **منفصل تمامًا** عن boundary محتوى الصفحة. عطل في المساعد يجب أن يُخفي الودجت فقط (أو يعرض زر "أعد تحميل المساعد" صغيرًا) بينما تستمر شاشة POS/الطلبات/إلخ تعمل بلا انقطاع.

### د. لا تلمس الغلاف الأعلى في main.tsx
يبقى كما هو — هو خط الدفاع الأخير لأي خطأ يفلت من كل ما سبق (مثل عطل في الـ Router نفسه أو تهيئة i18n).

---

## 2️⃣ المرحلة 2: تحصين الخادم بشبكة أمان أخيرة

### أ. معالجات مستوى العملية (process-level)
في `server.ts`، بعد إنشاء `app` وقبل `app.listen`/التصدير، أضف:

```ts
process.on('unhandledRejection', (reason) => {
  logError(reason instanceof Error ? reason : new Error(String(reason)), { source: 'unhandledRejection' });
  // لا تستدعِ process.exit() هنا — هذا التطبيق يعمل كدالة Vercel serverless
  // (عبر api/index.js)، وإنهاء العملية غير مجدٍ/ضار في هذا السياق.
  // إن كان server.ts يُشغَّل أيضًا كخادم مستقل دائم (self-host)، تحقّق مع
  // الفريق هل يُفضَّل هناك إعادة تشغيل مُدارة (عبر PM2/systemd) بدل الاستمرار.
});
process.on('uncaughtException', (err) => {
  logError(err, { source: 'uncaughtException' });
});
```

(استخدم دالة اللوق الموجودة فعليًا في المشروع — نفس ما يُستخدم داخل الـ try/catch الحالية في server.ts — لا تُنشئ آلية تسجيل جديدة.)

### ب. middleware معالجة أخطاء عام في نهاية الملف
بعد كل تعريفات المسارات (بعد آخر `app.get('*', ...)`)، أضف:

```ts
app.use((err: any, req: express.Request, res: express.Response, next: express.NextFunction) => {
  logError(err, { source: 'globalErrorHandler', path: req.path });
  if (res.headersSent) return next(err);
  res.status(500).json({ error: 'حدث خطأ غير متوقع، حاول لاحقًا' });
});
```

هذا لا يغيّر سلوك أي مسار يعالج أخطاءه بنفسه بالفعل (الأغلبية) — فقط يلتقط ما يفلت منها.

### ج. تحقق سريع من `authMiddleware.ts`
افتح `src/server/middleware/authMiddleware.ts` وتأكد أن **كل** دالة مُصدَّرة منه (وليس فقط بعضها) محاطة بـ try/catch تستدعي `next(err)` أو ترجع استجابة خطأ صريحة، بما أنها تعمل على أغلب المسارات المحمية.

---

## 3️⃣ المرحلة 3: تحقق إلزامي قبل الإغلاق

1. أضف مؤقتًا `throw new Error('اختبار عزل الأعطال')` داخل صفحة واحدة فقط (مثل التقارير)، وتأكد:
   - تظهر بطاقة خطأ صغيرة **داخل مكان الصفحة فقط**، لا شاشة كاملة.
   - التنقل لصفحة أخرى (مثل الطلبات) **يعمل بلا أي مشكلة** بدون إعادة تحميل.
   - احذف كود الاختبار فور التأكد.
2. كرّر نفس الاختبار داخل مكوّن المساعد الذكي تحديدًا — تأكد أن الصفحة المحيطة تبقى تعمل والودجت فقط يظهر بحالة خطأ.
3. اختبر أن الغلاف الأعلى (`main.tsx`) لا يزال يعمل: ضع خطأ مؤقت خارج كل الـ boundaries الفرعية (مثلاً في `Router` نفسه) وتأكد أن شاشة الخطأ الكاملة القديمة لا تزال تظهر كملاذ أخير.
4. أرسل طلبًا بجسم JSON فاسد عمدًا لأي API endpoint وتأكد أن السيرفر يرجع رد 500 منظّم بدل أن يتعطل أو يرجع صفحة HTML افتراضية.

## ✅ معايير القبول (Definition of Done)

- [ ] خطأ داخل أي صفحة واحدة لا يُسقط باقي الصفحات — التنقل يبقى ممكنًا
- [ ] خطأ داخل ودجت المساعد الذكي لا يُسقط الصفحة المحيطة
- [ ] الغلاف الأعلى في `main.tsx` لم يُحذف — يبقى خط الدفاع الأخير كما هو
- [ ] `server.ts` يحتوي الآن `process.on('unhandledRejection')` و`process.on('uncaughtException')` و middleware معالجة أخطاء عام واحد في النهاية
- [ ] لا تغيير في سلوك أي مسار كان يعالج أخطاءه بنفسه بالفعل (الأغلبية) — الإضافات فقط شبكة أمان لما يفلت
- [ ] كل نتائج اختبارات المرحلة 3 موثّقة (نجح/فشل) قبل اعتبار المهمة منتهية
