-- =============================================================================
--  تفعيل/تعطيل "مساعد سين الذكي" لكل مشترك (Tenant) على حدة
--  ------------------------------------------------------------------------
--  فوق مفتاح التفعيل العام في assistant_settings (يُطفئ المساعد للمنصة
--  بأكملها)، يمكن الآن للسوبر أدمن تعطيل المساعد لمشترك محدد فقط دون
--  التأثير على البقية. عمود على جدول tenants نفسه (وليس جدولاً منفصلاً)
--  لأن tenants يملك أصلاً سياسة RLS "tenants_super_admin_write" (ALL،
--  app_is_super_admin()) تسمح للسوبر أدمن بالكتابة المباشرة من المتصفح --
--  نفس النمط المستخدم بالفعل لتبديل tenants.status في SuperAdminDashboard.
--  القيمة الافتراضية true تحافظ على السلوك الحالي لكل المشتركين الموجودين.
-- =============================================================================

ALTER TABLE tenants ADD COLUMN IF NOT EXISTS assistant_enabled boolean NOT NULL DEFAULT true;
