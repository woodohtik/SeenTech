# مهمة: رفع تطبيق العميل من "يعمل" إلى "يليق بعلامة سِين" — تصميم واحترافية

## 🎭 الدور
تصرف كـ **مصمّم منتج أول (Senior Product Designer)** يقود العمل بمنهجية مهارة `frontend-design` المثبَّتة (Brainstorm → Plan → Critique → Build → Critique مرة ثانية) — لا تنتقل لكتابة كود مباشرة. استعن أثناء ذلك بـ `ui-ux-pro-max` (بحث في الخطوط/الأيقونات/الألوان/الحركة) و`design-system` (توكِنز التصميم) المثبَّتتين. بعد التصميم، نفّذ كمبرمج، ثم راجع كمراجع (تباين ألوان، RTL، حالتا الوضع الفاتح/الداكن) قبل الإغلاق.

---

## 🎯 السياق — الوضع الفعلي الآن (فحصته للتو، ليس افتراضًا)

الخبر الجيد: **تطبيق العميل مكتمل وظيفيًا فعلاً** — `OrderTracking.tsx` و`MyOrdersHome.tsx` (شاشة "طلباتي") يعملان، محفوظان محليًا عبر `trackedOrders.ts`، push حقيقي عبر `pushNotificationsCapacitorCustomer.ts`، وحتى ميزة لم أطلبها أنت وأضافها من نفّذ المهمة السابقة بذكاء: **بحث يدوي عن الطلب برقم الفاتورة + آخر 4 أرقام من الجوال** (`lookupOrderByInvoiceAndPhone`) لمن يفتح التطبيق بلا رابط جاهز — لمسة جيدة، أبقِها.

المشكلة: **كل هذا مبني بأقل تصميم ممكن ليعمل، لا ليُقنع**. أمثلة فعلية من الكود الحالي:
- **لا نظام تصميم مشترك**: نفس الثابتين `NAVY = '#1F3A5F'` و`BLUE = '#2E75B6'` **مُعرَّفان يدويًا بشكل منفصل** في `OrderTracking.tsx` وفي `MyOrdersHome.tsx` — أي تغيير لون العلامة مستقبلاً يعني تعديل ملفين (وأكثر لاحقًا) بدل مصدر واحد.
- **الخط**: `fontFamily: 'Arial, sans-serif'` — خط نظام عام لا علاقة له بهوية "سِين"، ولا محسَّن لقراءة العربية.
- **حالة التحميل**: نص "جارٍ التحميل..." فقط، لا Skeleton — يبدو مكسورًا للحظة قبل أن يبدو بطيئًا.
- **الخط الزمني (Timeline)**: أرقام داخل دوائر رمادية/زرقاء فقط، لا أيقونة واحدة تعبّر عن "قص" أو "خياطة" أو "تطريز" — بينما `ui-ux-pro-max` المثبَّت عندك فيه كتالوج 105 أيقونة جاهز لم يُستخدَم هنا إطلاقًا.
- **لا وضع داكن**: عميل يفتح رابط التتبّع ليلاً يحصل على خلفية بيضاء ساطعة قسرًا.
- **حالة "طلباتي" الفارغة**: نص باهت فقط بلا أي عنصر بصري يرحّب أو يوجّه.
- **لا شخصية**: لا صورة واحدة للمنتج الفعلي (قماش، قطعة قيد التفصيل) — فقط شعار المحل الصغير. هذا نظام تفصيل ملابس؛ "رؤية القطعة" فرصة تفاعل عاطفي حقيقية غير مستغَلة.

**تنبيه مهم قبل أن تخلط**: يوجد في المشروع مكوّن `Referral.tsx` — لكنه **برنامج إحالة بين أصحاب المحلات (tenants) لبعضهم للاشتراك في منصة سِين نفسها** (300 ر.س لكل محل يُحال ويشترك)، **وليس** برنامج ولاء لعملاء المحلات النهائيين. لا تخلط بينهما ولا تحاول "ربط" تطبيق العميل به — أي فكرة نمو/مشاركة لعميل المحل النهائي في هذه المهمة هي **ميزة جديدة منفصلة تمامًا**، ليست إعادة استخدام لشيء موجود.

---

## 📋 خطة العمل

### المرحلة 1 — `brainstorming` + بحث تصميم فعلي (لا تخمين)
قبل أي كود، ابحث فعليًا:
```bash
python "${CLAUDE_PLUGIN_ROOT}/.claude/skills/ui-ux-pro-max/scripts/search.py" "arabic rtl font pairing" --domain typography
python "${CLAUDE_PLUGIN_ROOT}/.claude/skills/ui-ux-pro-max/scripts/search.py" "tailoring sewing fabric icons" --domain icons
python "${CLAUDE_PLUGIN_ROOT}/.claude/skills/ui-ux-pro-max/scripts/search.py" "progress timeline stepper" --domain ux
python "${CLAUDE_PLUGIN_ROOT}/.claude/skills/ui-ux-pro-max/scripts/search.py" "micro interaction motion duration" --domain motion
```
حدّد بناءً على النتائج: خط عربي حقيقي (مرشّح منطقي: IBM Plex Sans Arabic أو Cairo أو Tajawal — تحقّق من نتائج البحث لا افتراضًا مني)، أيقونات فعلية لكل مرحلة (قص/خياطة/تطريز/كي وتغليف/جاهز)، ومدد حركة (durations) للانتقالات.

### المرحلة 2 — ملف توكِنز مشترك واحد (يُصلح الفجوة الحقيقية المكتشفة أعلاه)
أنشئ `src/lib/customerAppTheme.ts` واحدًا يصدّر: الألوان (فاتح + داكن، مبنية على نفس NAVY/BLUE الحاليين لا استبدالهما — هذه هوية سِين الفعلية، لا تخترع لونًا جديدًا)، الخط المُختار من المرحلة 1، مقياس المسافات وشعاع الحواف من `design-system/references/primitive-tokens.md`، مدد الحركة. **بعدها فقط**: بدّل `OrderTracking.tsx` و`MyOrdersHome.tsx` ليستوردا منه بدل تعريف NAVY/BLUE محليًا في كل ملف.

### المرحلة 3 — إعادة تصميم `OrderTracking.tsx`
- استبدل الدوائر المرقّمة بخط زمني عمودي بأيقونة حقيقية لكل مرحلة (من نتائج بحث المرحلة 1)، مع تعبئة تدريجية متحركة (لا قفزة لونية فورية) بين المرحلة الحالية والسابقة.
- استبدل تاريخ التسليم الخام بعبارة نسبية ودّية ("متبقّي يومان" / "اليوم") مع أيقونة تقويم، والتاريخ الكامل كنص ثانوي أصغر تحتها.
- Skeleton loading بدل نص "جارٍ التحميل" (شكل بطاقة رمادية نابضة بنفس أبعاد المحتوى الحقيقي).
- وضع داكن كامل عبر توكِنز المرحلة 2 (`prefers-color-scheme`).
- حالة "تم التسليم": لحظة احتفالية بسيطة (أيقونة/رسم متحرك خفيف عند الوصول لهذه الحالة تحديدًا، لا في كل الحالات) — راجع مهارة `frontend-design` لتفادي المبالغة ("AI-slop": لا تفجّر confetti بلا داعٍ، لمسة واحدة أنيقة تكفي).

### المرحلة 4 — إعادة تصميم `MyOrdersHome.tsx`
- حالة "طلباتي" الفارغة: رسم/أيقونة كبيرة ودودة + عنوان واضح + زر "أضف طلبًا" بارز بدل نص باهت فقط.
- بطاقات القائمة: أضف مؤشر حالة بصري أوضح (شريط لون جانبي رفيع حسب الحالة، لا فقط نص pill)، وSkeleton أثناء تحميل كل بطاقة بدل نص "جارٍ التحميل" عام لكل القائمة.
- إن كان معقولًا تقنيًا داخل Capacitor: أضف سحب-للتحديث (pull-to-refresh) — يعطي إحساس تطبيق أصلي حقيقي لا صفحة ويب مغلَّفة.

### المرحلة 5 — فكرة نمو جديدة (اختيارية، منفصلة تمامًا عن Referral.tsx كما وُضِّح أعلاه)
عند وصول حالة "تم التسليم": بطاقة مشاركة قابلة للتوليد (صورة بسيطة: شعار سِين + اسم المحل + "تم التسليم ✓") يشاركها العميل في واتساب/الحالة — نفس فكرة "وعي مجاني بالعلامة" المذكورة في تعليق الكود الأصلي لـ`OrderTracking.tsx`، لكن ميزة فعلية بدل تعليق نوايا فقط. لا تنفّذ هذه المرحلة إن كانت الأولوية الوقت — علّق عليها بوضوح كمؤجَّلة إن أُجِّلت.

### المرحلة 6 — مراجعة (مراجع)
1. تباين الألوان (WCAG AA) لكل نص فوق أي خلفية جديدة، في الوضعين الفاتح والداكن.
2. اختبار RTL كامل على جهاز أندرويد حقيقي (لا محاكي فقط) — تأكد أن الأيقونات الاتجاهية (إن وُجدت) تنعكس صحيحًا.
3. `/code-review high` على كل الملفات المعدَّلة.
4. تأكد أن `pushNotificationsCapacitorCustomer.ts` ومنطق `trackedOrders.ts`/`lookupOrderByInvoiceAndPhone` **لم يتغيّر سلوكهما إطلاقًا** — هذه مهمة تصميم بصري بحت، لا تلمس المنطق الوظيفي القائم.

---

## ✅ معايير القبول (Definition of Done)

- [ ] لا تكرار لثوابت الألوان — مصدر واحد فقط (`customerAppTheme.ts`)
- [ ] خط عربي حقيقي مُختار ببحث فعلي من `ui-ux-pro-max`، لا Arial
- [ ] كل مرحلة في الخط الزمني لها أيقونة مميّزة، لا رقم فقط
- [ ] Skeleton loading في كلا الشاشتين، لا نص "جارٍ التحميل" مجرّد
- [ ] وضع داكن يعمل فعليًا في كلا الشاشتين
- [ ] حالة "طلباتي" الفارغة مصمَّمة بعنصر بصري وCTA واضح
- [ ] لا أي تغيير في منطق `trackedOrders.ts` / `pushNotificationsCapacitorCustomer.ts` / البحث اليدوي بالفاتورة ورقم الجوال
- [ ] تباين الألوان يجتاز WCAG AA في الوضعين
- [ ] اختُبر فعليًا على جهاز أندرويد حقيقي (ليس متصفح ديسكتوب فقط) — لقطات شاشة قبل/بعد موثَّقة
- [ ] `/code-review high` نُفِّذ على كل التعديلات
