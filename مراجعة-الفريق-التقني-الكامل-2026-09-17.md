# سِين — مراجعة الفريق التقني الكامل (2026-09-17)

## 🎯 ملخص تنفيذي

- **عزل المستأجرين (RLS) في حالة غير مؤكَّدة**: دالة `app_current_tenant_id()` التي عليها مبنية سياسات RLS لنحو 30 جدولاً موجودة بنسختين متضاربتين — نسخة في migrations المتتبَّعة تعتمد على متغيّر جلسة لا يضبطه أي كود فعلياً (ترجع NULL دائماً)، والنسخة الصحيحة الوحيدة موجودة فقط في سكربت غير متتبَّع (`scripts/legacy-oneoff/fix-rls.sql`) لم يُطبَّق قط كـ migration رسمية على الإنتاج. **حرج — أمان/قاعدة بيانات**.
- **ربط زاتكا الفعلي بخوادم Clearance/Reporting غير موجود**: رغم بنية تحتية جزئية حقيقية (توقيع ECDSA، سلسلة PIH، تخزين CSID مشفَّر)، لا توجد أي فاتورة — ولو تجريبياً — تمر فعلياً بمسار الإرسال لزاتكا؛ إضافة إلى وجود تطبيقين متوازيين مهجورين لنفس المنطق. **حرج — امتثال/بنية**.
- **الفواتير الضريبية الصادرة قابلة للتعديل/الحذف على مستوى القاعدة**: لا يوجد أي قيد RLS أو trigger يمنع `UPDATE`/`DELETE` مباشر على `tax_invoices` من أي موظف نشط تابع لنفس المستأجر عبر واجهة Postgrest. **حرج — امتثال**.
- **صفر اختبارات لأهم مسارات المنطق الحرج**: مسار الدفع الكامل في POS.tsx، مصادقة/تفويض تعدد المستأجرين (authMiddleware.ts)، ومنطق تمييز فشل الشبكة في outbox.ts — كلها بلا أي اختبار وحدة أو E2E. **حرج — جودة**.
- **انجراف مخطط قاعدة بيانات حقيقي**: جداول/Views مستخدَمة فعلياً في الكود (`roles_permissions`, `tenant_activation` وViews تحليلية) لا وجود لها في أي migration بالمستودع، بالإضافة لـ migration ستفشل حتماً على أي `supabase db reset` نظيف. **حرج — قاعدة بيانات**.

---

## 🔴 نتائج حرجة (تتطلب إجراءً فورياً)

1. **عزل المستأجرين غير قابل للتحقق من الكود وحده** — `app_current_tenant_id()` في `supabase/legacy-setup/wdooh-database-schema.sql:33-39` و`supabase/migrations/20260815090000_supabase_auth_cutover.sql` (المتتبَّعة) تعتمد على `current_setting('app.current_tenant_id', true)` الذي لا يضبطه أي كود في المشروع — بينما النسخة الصحيحة فعلياً (تشتق tenant_id من JWT عبر `staff.tenant_id`) موجودة فقط في `scripts/legacy-oneoff/fix-rls.sql:33-64`، غير المتتبَّع كـ migration ومُوثَّق ذاتياً في `20260824090000_tenants_onboarding_insert_policy.sql:2-16` بأنه "طُبِّق يدوياً على staging فقط، لم يُطبَّق على production قط". **[security-reviewer]**
   ملاحظة ربط: compliance-reviewer اعتبر عزل المستأجرين "سليماً" استناداً لنفس ملف `fix-rls.sql` كمرجع — أي أن هذا التقييم الإيجابي يعتمد على افتراض أن هذا الملف غير المتتبَّع هو ما يعمل فعلياً على الإنتاج، وهو بالضبط ما لا يمكن تأكيده من الكود وحده. **[security-reviewer + compliance-reviewer]**
   **التوصية العاجلة:** التحقق المباشر من نص `app_current_tenant_id()` الحالي على production عبر `pg_proc`، وتحويل `fix-rls.sql` إلى migration رسمية idempotent فوراً.

2. **لا ربط فعلي بين الكود وخوادم زاتكا (Clearance/Reporting)** — `src/services/zatcaPhase2Service.ts:150-152` لا تزال `signInvoice()` ترمي `TODO(ZATCA)` عمداً، ولا يوجد أي استدعاء لـ`processPhase2Invoice`/`clearInvoice`/`reportInvoice` من أي مسار تشغيلي في المشروع. **[compliance-reviewer]**
   إضافة لذلك: يوجد تطبيقان متوازيان مستقلان وغير متصلين لنفس منطق UBL XML/التوقيع (`src/services/zatcaPhase2Service.ts` القديم مقابل `src/server/zatca*.ts` الأحدث والموثَّق) — كلاهما غير مستخدَم من أي route فعلي، مما يخلق التباساً لأي عمل مستقبلي. **[architecture-reviewer]**
   حقل `invoice_hash`/`previous_invoice_hash` في `tax_invoices` يُملأ حالياً بقيمة "placeholder" موثَّقة ذاتياً كغير حقيقية (ليست هاش XML موقَّع فعلي) — خطر أن يُفهَم خطأً كامتثال فعلي. **[architecture-reviewer]**

3. **الفواتير الضريبية قابلة للتعديل/الحذف مباشرة على مستوى RLS** — `supabase/legacy-setup/wdooh-database-schema.sql:1045-1130`: `tax_invoices` غير مُدرَجة في قائمة استثناءات "الجداول الشبيهة بسجل تدقيق غير قابلة للتعديل" (بعكس `audit_logs`, `security_logs`, `stock_ledger`)، ولا يوجد أي trigger `BEFORE UPDATE/DELETE` يمنع التعديل بعد الإصدار. أي موظف نشط تابع لنفس المستأجر يملك تقنياً صلاحية `UPDATE`/`DELETE` مباشرة عبر Postgrest، رغم أن واجهة المستخدم لا تعرض حالياً أي زر لذلك. أي إصلاح يجب أن يستثني `trialService.ts:114` (حذف مشروع لفواتير تجريبية `is_test=true` فقط). **[compliance-reviewer]**

4. **صفر اختبارات لأهم منطق حرج في المشروع** — المستودع بأكمله يحتوي 3 ملفات اختبار وحدة (utils، zatcaSigning، zatcaInvoiceXml) + ملف E2E واحد لا يلمس POS إطلاقاً:
   - `src/components/POS.tsx` (2753 سطر، كامل مسار الدفع) — صفر اختبارات، بما في ذلك حارس "منع فاتورة برقم ضريبي وهمي" (خط ~946) وهو إصلاح لثغرة قانونية سبق وقوعها فعلياً بلا أي regression test يحميه.
   - `src/server/middleware/authMiddleware.ts` (فصل المستأجرين/الصلاحيات) — صفر اختبارات رغم سجل تاريخي لثغرة تجاوز صلاحيات أُزيلت يدوياً في نفس الملف.
   - `src/lib/offline/outbox.ts` — منطق تمييز فشل الشبكة عن رفض شرعي (`isNetworkFailure`) بلا اختبار، رغم أنه دالة منطقية خالصة يسهل اختبارها.
   - لا يوجد أي اختبار E2E لبيع POS كامل من البداية للنهاية. **[qa-reviewer]**

5. **انجراف مخطط قاعدة بيانات** — جداول/Views مستخدَمة فعلياً في الكود بلا أي `CREATE TABLE`/`CREATE VIEW` في كامل المستودع (لا migrations، لا legacy-setup، لا src/db): `roles_permissions` (`permissionService.ts`, `RolePermissionsSettings.tsx`)، `tenant_activation` + 4 Views تحليلية (`activationService.ts`)، `expenses` (`AdminDashboard.tsx`). على أي بيئة جديدة تُبنى من migrations فقط، هذه الميزات تفشل صامتاً أو تعرض بيانات فارغة/خاطئة بلا أي خطأ ظاهر.
   إضافة لذلك: `supabase/migrations/20260825000000_fix_inventory_adjustments_rls.sql:10-24` ينفّذ `CREATE POLICY` على `inventory_adjustments`/`adjustment_items` رغم أن `CREATE TABLE` لهذين الجدولين موجود فقط في `src/db/schema_inventory_adjustments.sql` خارج مجلد migrations كلياً — **هذا الملف سيفشل حرفياً على أي `supabase db reset` نظيف**، موقفاً كل الـ34 migration التالية له زمنياً. **[database-reviewer]**

---

## 🟠 نتائج عالية الأولوية

- **تخزين PIN الموظفين كنص صريح** — `server.ts:966-973` يخزّن/يقارن PIN الموظفين كنص صريح 4 أرقام (قرار منتج موثَّق صراحة في الكود، ليس سهواً)، وتُعاد هذه القيم صراحة لـ owner/super_admin عبر `/api/staff/pins`. أي تسرّب لمفتاح service_role أو نسخة احتياطية يكشف فوراً أرقام دخول كل الموظفين عبر كل المستأجرين. **[security-reviewer]**
- **جلب بيانات غير محدود (`select('*')` بلا `.limit()`/`.range()`)** على 52 موقعاً عبر المشروع، أبرزها `DashboardOwner.tsx` (10 مواضع)، `Reports.tsx`، `POS.tsx:353-356`، `Orders.tsx`، `InventoryManager.tsx`، `Customers.tsx`، `Suppliers.tsx`، `Sales.tsx` — تُجلب الجداول كاملة تاريخياً لكل تحميل تبويب/وردية بلا سقف، ما يتناسب سوءاً مع عمر الحساب لا حجم الصفحة المعروضة. **[performance-reviewer + architecture-reviewer]**
- **فلترة/بحث بلا `useMemo` ولا debounce على مسارات الاستخدام الأكثر تكراراً**: `POS.tsx:637` (فلترة كتالوج المخزون أثناء البيع الفعلي)، `Orders.tsx:1474,2457,2463` (فلترة مزدوجة + `motion.div layout` على قائمة غير محدودة)، `Customers.tsx:642,1017`، `InventoryManager.tsx:414`. **[performance-reviewer]**
- **`html2pdf`/jsPDF مستورَد بشكل ثابت في صفحة عامة يفتحها العملاء** — `src/pages/PublicInvoice.tsx:7,102` يستورد المكتبة كاملة (993KB خام/284KB gzip، أكبر chunk في المشروع) رغم استخدامها فقط عند الضغط على "تنزيل PDF"، على صفحة تُفتح من روابط واتساب على شبكات جوال. **[performance-reviewer]**
- **`AdminDashboard.tsx:76-120`** — 4 استعلامات مستقلة تماماً (`orders`, `expenses`, `inventory_items`, `staff`) بـ`await` متسلسل بدل `Promise.all`. **[performance-reviewer]**
- **قفل PIN دون اتصال والتصعيد الزمني بلا أي اختبار** (`offlinePinAuth.ts`) — بما في ذلك عدم اختبار انتهاء صلاحية القفل، وتجاهل صامت لإدخالات الكاش بالصيغة القديمة بلا ملح. **[qa-reviewer]**
- **مهلة الـ24 ساعة القانونية لزاتكا (`getZatcaApproachingEntries` في outbox.ts) بلا أي اختبار** — خطر تجاوز صامت للمهلة الشرعية لفاتورة B2C إذا بقي الجهاز دون اتصال طويلاً. **[qa-reviewer]**
- **صلاحيات UPDATE/DELETE على جدول `customers` غير مقيَّدة بالدور الوظيفي** — `supabase/legacy-setup/wdooh-database-schema.sql:1045-1112`: أي كاشير نشط يملك تقنياً صلاحية حذف/تعديل سجل عميل كامل مباشرة عبر Postgrest، بلا تمييز دور. **[compliance-reviewer]**
- **فشل وقت تشغيل محتمل لتحويل وحدات القماش** — `record_uom_conversion` في `20260910020000_backfill_inventory_fix.sql:231-240` تُدرِج في `uom_conversion_logs` بلا تحقق من وجوده؛ الجدول معرَّف فقط خارج migrations (`src/db/schema_fabric_uom.sql`). **[database-reviewer]**
- **قيد الرصيد السالب على `branch_inventory` قد لا يكون مُفعَّلاً فعلياً على الإنتاج** — `ADD CONSTRAINT chk_branch_inventory_non_negative` مغلَّف بمعالجة استثناء تكتفي بـ`RAISE WARNING` صامت إن وُجدت صفوف سالبة مسبقاً. **[database-reviewer]**
- **انجراف متغيرات البيئة** — متغيرات مستخدَمة فعلياً في الكود وغائبة عن `.env.example`: `ASSISTANT_ENCRYPTION_KEY` (يرمي خطأ فوري عند الغياب)، `PAYMENT_PROVIDER`/`MOYASAR_SECRET_KEY`/`PAYMOB_API_KEY` (فشل دفع صامت)، `VITE_FIREBASE_VAPID_KEY`، `VITE_GOOGLE_CLIENT_ID`، `VITE_CUSTOMER_API_ORIGIN`. **[devops-reviewer]**
- **`VITE_CUSTOMER_API_ORIGIN` بقيمة افتراضية `staging.seentech.io` مكتوبة في كود تطبيق العميل على أندرويد** (`src/lib/apiBase.ts:15`) — بناء إنتاج بلا تحديد هذا المتغيّر يُشحَن صامتاً متصلاً بـ staging بدل الإنتاج. **[devops-reviewer]**
- **لا توثيق مركزي للنشر عبر الأهداف المتعددة** (ويب/Vercel، Android staff/customer، print-agent، kiosk-print)، و`README.md` الجذري يصف بنية Firebase-only قديمة لا تعكس الواقع الحالي (Supabase+Vercel+Capacitor). **[devops-reviewer]**
- **"lint" في CI هو type-check فقط (`tsc --noEmit`) وليس ESLint فعلياً**، ولا يوجد `env:` block في CI فيمنعه من اكتشاف انجراف متغيرات البيئة أعلاه. **[qa-reviewer + devops-reviewer]**

---

## 🟡 نتائج متوسطة/تحسينات

- **تكرار منطق حساب الضريبة (VAT)** بين `TaxInvoice.tsx:101-126` و`SimplifiedTaxInvoice.tsx:95-120` (معدل ثابت 15% بدل قراءة `vatRate` الفعلي)، وبين `POS.tsx:861-890` و`Orders.tsx:474-502` (تطبيقان مستقلان لنفس معادلة inclusive/exclusive/exempt). **[architecture-reviewer]**
- **عدم اتساق نمط جلب البيانات** — `TaxInvoices.tsx`/`PurchaseOrders.tsx` (بيانات مالية حساسة) لا تستخدم react-query/realtime بعكس بقية الشاشات؛ بالمقابل `POS.tsx`/`Sales.tsx`/`DashboardToday.tsx`/`AdminDashboard.tsx` لا تستخدم react-query إطلاقاً (إعادة جلب كامل من الصفر في كل mount). **[architecture-reviewer + performance-reviewer]**
- **مكوّن `DataTable.tsx` عام (165 سطر) مبني بالكامل لكن غير مستخدَم إطلاقاً** — كل شاشة تعيد بناء منطق العرض/التحديد الجماعي يدوياً. **[architecture-reviewer]**
- **تصادم رقم الفاتورة بين جهازين** — `generateOrderNumber()` في `src/lib/utils.ts:36-45` يعتمد عداداً في ذاكرة المتصفح يُصفَّر عند كل تحميل صفحة؛ تصادم محتمل بين كاشيرين على جهازين مختلفين يُسقِط بيعاً مدفوعاً فعلياً بلا إعادة محاولة تلقائية. **[database-reviewer]**
- **ثغرة حد إشعارات الدائن** — `check_sales_return_total` قد لا يجد `v_invoice_total` (NULL) إذا مرَّر الكود `order.id` بدل معرّف فاتورة حقيقي، فلا يُفرَض أي حد أعلى على مبلغ الاسترجاع. **[database-reviewer]**
- **`WHEN OTHERS` واسع جداً في `create_pos_sale`** يبتلع أي خطأ (حتى غير المتعلق بالمخزون) ويحوّله لـ"تعارض مخزون" بصمت. **[database-reviewer]**
- **فهرسة مفقودة** على `withdrawal_requests.tenant_id` و`referrals.referrer_tenant_id` المستخدَم فعلياً في سياسة RLS. **[database-reviewer]**
- **`supplier_transactions` غير متّسق بين الأجهزة** — عند فشل الاستعلام يُخزَّن السجل محلياً (`localStorage`) لكل جهاز على حدة. **[database-reviewer]**
- **`inventory_adjustments`/`InventoryAdjustment.tsx:452-495`** — منطق حساب الفروقات الجردية مضمَّن مباشرة داخل مكوّن React (1471 سطراً) بدل خدمة قابلة للاختبار. **[architecture-reviewer]**
- **`tenant_whatsapp_sessions` جدول إنتاج كامل بلا أي كود مستهلك** — سكافولد من مشروع متوقف (مذكرة داخلية مؤكِّدة)، يستحق توثيق حالة "متوقف" أو الإزالة. **[architecture-reviewer + compliance-reviewer]**
- **نطاق `globPatterns` الواسع لـService Worker** — 102 ملفاً/7.2MB يُنزَّل في الخلفية لكل عميل بما فيها chunks إدارية نادرة الاستخدام (SaaS/SuperAdmin)، يُبطل جزئياً فائدة `React.lazy` على الشبكات المحدودة. **[performance-reviewer]**
- **سجلات outbox الفاشلة بعد 5 محاولات (`MAX_AUTO_RETRIES`)** تبقى معلَّقة (`status: 'failed'`) بلا اختبار لتنبيه واضح للمستخدم — احتمال فقدان بيانات مالية صامت. **[qa-reviewer]**
- **`print-agent/README.md`** يصف مشكلة (طابور في الذاكرة لا يعمل على Vercel) **تم إصلاحها فعلياً** (هجرة لـ Supabase موثَّقة في `printRelay.ts`) لكن التوثيق لم يُحدَّث، وقد يُضلِّل مطوّراً مستقبلياً. **[devops-reviewer]**
- **`Settings.tsx`** لا يفرض `required` على حقلي TRN/الاسم القانوني على مستوى النموذج نفسه (البوابة الفعلية موجودة في POS عند الإصدار، فهذه طبقة دفاع إضافية فقط، غير حرجة). **[compliance-reviewer]**

---

## 📊 حسب المجال

### الأمان

1. **عزل المستأجرين غير قابل للتحقق من الكود وحده** (حرج) — `app_current_tenant_id()` بنسختين متضاربتين، التفاصيل الكاملة في القسم الحرج أعلاه. الملفات: `supabase/legacy-setup/wdooh-database-schema.sql:33-39`، `supabase/migrations/20260815090000_supabase_auth_cutover.sql`، `scripts/legacy-oneoff/fix-rls.sql:33-64`، `supabase/migrations/20260824090000_tenants_onboarding_insert_policy.sql:2-16`، `src/services/supabase/client.ts:35-39`. سيناريو: إعادة بناء بيئة من migrations المتتبَّعة فقط تُنتج حجباً كاملاً لكل الجداول متعددة المستأجرين (فشل وظيفي)، ولا ضمان أن الإصلاح الصحيح الفعلي محفوظ من التآكل مستقبلياً — نفس فئة ثغرة "تسرّب بيانات بين المستأجرين" موثَّقة تاريخياً وأُصلحت مرة، لكن الإصلاح غير متتبَّع.
2. **تخزين PIN كنص صريح** (عالٍ) — `server.ts:966-973, 1002-1015, 1079-1109`. قرار منتج موثَّق، لكن يبقى نقطة ضعف بنيوية لبيانات اعتماد المصادقة عند أي تسرّب لمفتاح service_role أو نسخة احتياطية.
3. **نتائج تحقق إيجابي (لا ثغرة)**: `useRealtimeSync.ts` (فلتر tenant_id جانب العميل موثَّق كدفاع ثانوي فقط، الحد الحقيقي RLS)؛ `assistantCrypto.ts`/`zatcaCrypto.ts` (AES-256-GCM سليم، مفاتيح منفصلة)؛ `zatca_credentials`/`tenant_whatsapp_sessions` (RLS بلا سياسات = service-role فقط، تصميم صحيح)؛ `offlinePinAuth.ts` (PBKDF2 210,000 تكرار + ملح + قفل تصاعدي، حدوده موثَّقة صراحة ومقبولة)؛ حارس بيانات ضريبية وهمية في `POS.tsx:938-953` (أُصلح فعلياً، الباقي فقط fallback عرض تاريخي بأصفار)؛ `authMiddleware.ts` (تحقق JWT فعلي عبر JWKS، لا bypass بالبريد المُشفَّر بالكود)؛ `assistantTools.ts` (كل أداة مقيَّدة بـtenant_id من سياق مصادَق، `sanitizeForOrFilter()` يمنع كسر فلتر PostgREST)؛ لا أسرار مكتوبة صراحة في الكود، لا ملفات `.env` متتبَّعة تاريخياً.

### الأداء

1. `PublicInvoice.tsx:7,102` — استيراد html2pdf/jsPDF ثابت (993KB) على صفحة عامة يفتحها العملاء — **عالٍ**.
2. 52 موقع `select('*')` بلا `.limit()`/`.range()`، أبرزها `DashboardOwner.tsx` (10)، `Reports.tsx` (6)، `POS.tsx` (4)، `Orders.tsx` (4)، `InventoryManager.tsx` (4)، `Sales.tsx`/`Suppliers.tsx`/`Customers.tsx` (3 لكل) — **عالٍ لـDashboardOwner/Reports/POS، متوسط للباقي**.
3. `useQuery` مُستخدَم فعلاً في 7 ملفات (Suppliers, Customers, Reports, Orders, InventoryManager, DashboardOwner, Settings) — تحسّن حقيقي عن حالة سابقة مفترَضة — لكن لا `useMutation` إطلاقاً في المشروع (كل الكتابة عبر استدعاء supabase مباشر + `invalidateQueries` يدوي)، وPOS/Sales/DashboardToday/AdminDashboard/Dashboard لا تستخدم react-query إطلاقاً — **متوسط**.
4. فلترة بلا `useMemo` + بلا debounce: `Orders.tsx:1474,2457,2463` (فلترة مزدوجة O(n×m) + `motion.div layout` بلا windowing)، `Customers.tsx:642,1017`، `Suppliers.tsx:398`، `InventoryManager.tsx:414-427`، **`POS.tsx:637-643`** (الأولوية الأعلى — فلترة كتالوج المخزون بلا حد أثناء البيع الفعلي مع كل ضغطة حرف). بالمقابل `Reports.tsx` نموذج جيد (15 استخدام `useMemo` صحيح) — **عالٍ**.
5. Waterfalls قابلة للدمج بـ`Promise.all`: `AdminDashboard.tsx:76-120` (4 استعلامات مستقلة — عالٍ)، `FabricUomConversion.tsx:124-176` (متوسط)، `DashboardToday.tsx:57,66` (منخفض/متوسط)، `Customers.tsx:256-264` (متوسط). بالمقابل `Reports.tsx`/`DashboardOwner.tsx` يستخدمان `Promise.all` بشكل صحيح.
6. PWA/Service Worker: `maximumFileSizeToCacheInBytes` (5MB) كافٍ حالياً (أكبر ملف 993KB). لكن `globPatterns` الافتراضي يُنزِّل 102 ملف/7.2MB لكل عميل بما فيها chunks إدارية نادرة الاستخدام — **متوسط**.

### البنية والتصميم

1. تكرار حساب VAT بين `TaxInvoice.tsx`/`SimplifiedTaxInvoice.tsx` (معدل ثابت 15%) — **عالٍ**؛ وبين `POS.tsx`/`Orders.tsx` (تطبيقان مستقلان لنفس المعادلة) — **متوسط**.
2. جلب بيانات غير محدود على شاشات القوائم الرئيسية (`Orders.tsx`, `Customers.tsx`, `InventoryManager.tsx`) — **عالٍ** (يتفق مع نتائج performance-reviewer).
3. عدم اتساق نمط جلب البيانات: `TaxInvoices.tsx`/`PurchaseOrders.tsx` بدون react-query/realtime بعكس بقية الشاشات — **متوسط**.
4. `DataTable.tsx` عام غير مستخدَم إطلاقاً رغم اكتماله — **منخفض**.
5. تطبيقان متوازيان مهجوران لزاتكا Phase 2 (`zatcaPhase2Service.ts` القديم مقابل `server/zatca*.ts` الأحدث) — **متوسط**، توصية بحذف الأقدم.
6. حقل PIH بقيمة placeholder غير حقيقية مخزَّنة في الإنتاج — **متوسط-عالٍ للامتثال**.
7. `tenant_whatsapp_sessions` جدول كامل بلا مستهلك — **منخفض-متوسط**.
8. منطق تسوية المخزون (`InventoryAdjustment.tsx:452-495`) مضمَّن في مكوّن React بدل خدمة قابلة للاختبار — **متوسط**.
   ملاحظة إيجابية: `zatcaService.ts` re-export نظيف موحَّد لتكرار QR سابق (تم حله فعلاً)، و`printRelay.ts` تصميم جيد وموثَّق لانتقال من الذاكرة إلى Supabase.

### قاعدة البيانات

1. **انجراف مخطط حرِج** — `roles_permissions`, `tenant_activation` + 4 Views, `expenses` مستخدَمة في الكود بلا أي تعريف SQL في المستودع؛ `supplier_transactions` معالَج دفاعياً لكن يُنتج عدم اتساق بيانات بين الأجهزة.
2. **انجراف من نوع آخر** — `inventory_adjustments`/`adjustment_items` و`fabric_uoms`/`item_uom_conversions`/`uom_conversion_logs` معرَّفة فقط في `src/db/*.sql` خارج `migrations/`؛ `20260825000000_fix_inventory_adjustments_rls.sql` سيفشل حتماً على `supabase db reset` نظيف موقفاً 34 migration لاحقة — **حرِج**.
3. قيد `chk_branch_inventory_non_negative` قد لا يكون مُفعَّلاً فعلياً على الإنتاج إن وُجدت صفوف سالبة مسبقاً وقت تشغيل الترحيل — **عالٍ**.
4. دوال RPC الذرّية (`create_pos_sale`, `apply_stock_movement`, `increment_supplier_balance`) سليمة وذرّية فعلياً مع `FOR UPDATE`/`pg_advisory_xact_lock` صحيحة؛ استثناء مقصود وموثَّق: خصم المخزون يُبتلَع بـ`WHEN OTHERS` واسع (فاتورة قد تُسجَّل بمخزون غير مخصوم عند تعارض) — **منخفض**.
5. `check_sales_return_total` لا يفرض حداً أعلى إن كان `invoice_id` يشير لـ`orders.id` بدل فاتورة حقيقية — **متوسط**.
6. تصادم `order_number` بين جهازين (عداد في الذاكرة، لا تنسيق مركزي) يُسقِط بيعاً مدفوعاً بلا إعادة محاولة تلقائية — **متوسط-عالٍ**.
7. فهرسة مفقودة على `withdrawal_requests.tenant_id`/`referrals.referrer_tenant_id` — **منخفض**.
8. RLS مفعَّل على كل جدول جديد في migrations المتتبَّعة بلا استثناء (فحص إيجابي) — لا نتائج سلبية في Idempotency داخل `migrations/` نفسها؛ المشكلة الجذرية هي أن `wdooh-database-schema.sql` (المخطط الأساسي بالكامل) غير مُدار كـ migration متتبَّعة إطلاقاً.

### الجودة والاختبارات

- **التغطية الفعلية**: 3 ملفات اختبار وحدة + ملف E2E واحد فقط في كامل المستودع. صفر اختبارات لـ`POS.tsx`، `outbox.ts`، `offlinePinAuth.ts`، `stockConflicts.ts`/`cacheSync.ts`/`connectivity.ts`، `paymentService.ts`/`permissionService.ts`/`inventoryService.ts`/`staffService.ts`/`saasSecurityService.ts`، `authMiddleware.ts`، و`server.ts` — **حرِج**.
- لا اختبارات معطَّلة/متجاوَزة (`.skip`/`.todo`) — لكن هذا يعكس غياب التغطية أصلاً لا انضباطاً فيها.
- CI مفعَّل فعلياً على push/PR لـstaging وmain (type-check + الاختبارات الثلاثة + بناء التطبيقين + E2E)، لكنه لا يحمي عملياً من كسر منطق الدفع/المزامنة/الصلاحيات لأن لا اختبارات ذات معنى لها.
- حالات حدّية موثَّقة وغير مغطاة في POS.tsx: تمييز فشل شبكة/رفض شرعي (خط 1060-1078)، حارس منع فاتورة برقم ضريبي وهمي (خط ~946)، تعارض مخزون بين جهازين.
- `offlinePinAuth.ts`: جدول القفل التصاعدي، تجاهل كاش قديم بلا ملح.
- `outbox.ts`: مهلة الـ24 ساعة القانونية لزاتكا، سجلات فاشلة بعد `MAX_AUTO_RETRIES`.
- لا مسار مُختبَر لرفض فعلي من زاتكا (Clearance API) — لأن المسار نفسه غير منفَّذ بعد فعلياً (فجوة تنفيذ لا اختبار فقط).
- `authMiddleware.ts` (فصل tenantId، صلاحية super_admin) بلا أي اختبار رغم ثغرة تاريخية أُزيلت يدوياً في نفس الملف.
- لا اختبار E2E واحد يغطي بيع POS كامل من البداية للنهاية؛ الملف الوحيد الموجود يغطي فقط تتبع الطلب العام.
- ملاحظة إيجابية: الاختبارات الثلاثة الموجودة (zatcaSigning، zatcaInvoiceXml، utils) عالية الجودة فعلياً وتغطي حالات حدّية حقيقية.

### البنية التحتية والنشر

1. **انجراف متغيرات البيئة** (عالٍ) — `ASSISTANT_ENCRYPTION_KEY`, `PAYMENT_PROVIDER`/`MOYASAR_SECRET_KEY`/`PAYMOB_API_KEY`, `VITE_FIREBASE_VAPID_KEY`, `VITE_GOOGLE_CLIENT_ID`, `VITE_FIREBASE_DATABASE_URL`, `VITE_CUSTOMER_API_ORIGIN` مستخدَمة فعلياً وغائبة عن `.env.example`. لا اتجاه عكسي (كل متغير في `.env.example` له استخدام فعلي).
2. لا `env:` block في `ci.yml` — البناء ينجح بلا متغيرات بيئة فلا يكتشف CI أياً من حالات الانجراف أعلاه — **متوسط**.
3. **الأسرار**: لا سر حقيقي مكتوب في كود متتبَّع (فحص إيجابي شامل على كامل تاريخ git)؛ ملفات توقيع Android (`keystore.jks`/`keystore.properties`) موجودة على القرص لكن مُستثناة بشكل صحيح من git؛ `google-services.json` يحتوي مفتاح Firebase Android عام بطبيعته (يُستحسن التحقق من قيود API Key في Google Cloud Console).
4. CI/CD موجود وفعّال (type-check + اختبارات + بناء التطبيقين + E2E) لكن بلا خطوة نشر صريحة موثَّقة (على الأرجح تكامل Vercel المباشر مع GitHub، غير موثَّق في المستودع) — **متوسط**.
5. **تناثر معرفة النشر عبر الأهداف المتعددة** (عالٍ) — لا نقطة مركزية توثّق بناء/نشر ويب/Vercel/Android(staff+customer)/print-agent/kiosk-print؛ `README.md` الجذري يصف بنية Firebase-only قديمة لا تعكس الواقع (Supabase+Vercel+Capacitor+server.ts)؛ `android/`/`capacitor-customer/` بلا README.
6. `print-agent/README.md` يصف مشكلة (طابور بالذاكرة لا يعمل على Vercel) **تم إصلاحها فعلياً** لكن التوثيق لم يُحدَّث — قد يُضلِّل مطوراً مستقبلياً — **متوسط**.
7. **`VITE_CUSTOMER_API_ORIGIN` بقيمة افتراضية staging مكتوبة في كود تطبيق أندرويد المُشحَّن** (`src/lib/apiBase.ts:15`) — بناء إنتاج بلا تحديد المتغيّر يتصل صامتاً بـstaging — **عالٍ**.
8. PWA/Service Worker (`skipWaiting`/`clientsClaim`) مُعالَج بشكل صحيح وموثَّق بالسبب — لا مشكلة حالياً.

### الامتثال التنظيمي

1. **ربط زاتكا Phase 2 الفعلي غير موجود** (حرِج) — `signInvoice()` ترمي TODO عمداً، لا استدعاء فعلي لمسار الإرسال لخوادم زاتكا في أي مكان بالمشروع، رغم تقدّم بنيوي حقيقي (PIH chain موقَّع بـ`FOR UPDATE`، تخزين CSID مشفَّر AES-256-GCM). الهاش المحسوب حالياً هو لحقول نصية للفاتورة وليس هاش XML مقنَّن كما تتطلبه زاتكا فعلياً (موثَّق ذاتياً كقصور مؤقت).
2. **بيانات بائع/ضريبة وهمية — أُصلحت فعلياً** (منخفض) — `POS.tsx:938-953` يمنع الآن إتمام البيع بلا TRN/اسم قانوني حقيقيين؛ كل الظهورات المتبقية لرقم `300000000000003` هي placeholder نصي في حقول إدخال فقط. الفجوة المتبقية الوحيدة: `Settings.tsx` لا يفرض `required` على مستوى النموذج (دفاع إضافي فقط، غير حرج).
3. **استثناء B2B من المزامنة دون اتصال لا يزال قائماً بلا تراجع** (لا إجراء مطلوب حالياً) — `POS.tsx:1064-1078`/`outbox.ts:10` موثَّق كقرار مالك صريح.
4. **عزل تعدد المستأجرين** يبدو سليماً استناداً لملف `fix-rls.sql` — **لكن انظر التنبيه في القسم الحرج أعلاه**: هذا التقييم يعتمد على ملف غير متتبَّع لم يُطبَّق رسمياً على الإنتاج حسب تقرير الأمان.
5. **صلاحيات UPDATE/DELETE على `customers` غير مقيَّدة بالدور الوظيفي** (متوسط-عالٍ) — أي كاشير نشط يملك تقنياً صلاحية حذف/تعديل سجل عميل مباشرة عبر Postgrest.
6. واتساب: التطبيق يستخدم `wa.me` يدوياً فقط، لا نقل بيانات آلي لطرف ثالث — لا مخاطرة حالية. جدول `tenant_whatsapp_sessions` خامل وغير مستخدَم (منخفض، ملاحظة نظافة فقط).
7. **الاحتفاظ بالسجلات — فجوة جوهرية** (حرِج) — `tax_invoices` بلا أي حاجز RLS/trigger يمنع UPDATE/DELETE بعد الإصدار (التفاصيل الكاملة في القسم الحرج أعلاه)؛ أهم فجوة لم يذكرها تقرير الجاهزية السابق إطلاقاً.

---

## ✅ خطوات تالية مقترحة

1. التحقق الفوري من نص `app_current_tenant_id()` الحالي على production عبر `pg_proc`، وتحويل `scripts/legacy-oneoff/fix-rls.sql` إلى migration رسمية idempotent متتبَّعة.
2. إضافة قيد/trigger يمنع `UPDATE`/`DELETE` مباشر على `tax_invoices` من العميل (مع استثناء صريح لمسار `trialService.ts` عبر دالة SECURITY DEFINER تتحقق من `is_test=true`).
3. توثيق الجداول/الـViews الست الغائبة من migrations (`roles_permissions`, `tenant_activation` + Views, `expenses`) عبر `pg_dump --schema-only` من الإنتاج، وكتابة migration توفيقية واحدة قبل أي محاولة بناء بيئة نظيفة.
4. نقل `src/db/schema_inventory_adjustments.sql` و`src/db/schema_fabric_uom.sql` إلى `supabase/migrations/` بترتيب زمني صحيح قبل `20260825000000_fix_inventory_adjustments_rls.sql`.
5. إضافة قيد صلاحيات على مستوى الدور الوظيفي لعمليات UPDATE/DELETE على `customers`.
6. كتابة اختبارات وحدة لمنطق `outbox.ts` (`isNetworkFailure`, `getZatcaApproachingEntries`)، `offlinePinAuth.ts`، وregression test لحارس بيانات الضريبة الوهمية في `POS.tsx`؛ إضافة اختبار E2E واحد لبيع POS كامل بالتنسيق مع المستخدم (بلا حسابات اختبار تلقائية وفق قاعدة المشروع).
7. حسم مصير `zatcaPhase2Service.ts` (حذف الأقدم غير المختبَر) والإبقاء على `src/server/zatca*.ts` كنقطة الانطلاق الوحيدة الموثَّقة لأي استئناف مستقبلي لزاتكا Phase 2.
8. تحويل `html2pdf` في `PublicInvoice.tsx` إلى `import()` ديناميكي داخل معالج التنزيل فقط.
9. إضافة `useMemo`/debounce لقوائم الفلترة في `POS.tsx`, `Orders.tsx`, `Customers.tsx`, `InventoryManager.tsx`، وإضافة `.limit()`/`.range()` أو نافذة زمنية افتراضية لاستعلامات `select('*')` غير المحدودة (بالأولوية: DashboardOwner, Reports, POS).
10. استكمال `.env.example` بالمتغيرات الغائبة (`ASSISTANT_ENCRYPTION_KEY`, متغيرات الدفع, `VITE_CUSTOMER_API_ORIGIN`, إلخ)، وجعل بناء إنتاج تطبيق العميل يرفض (throw) بدل fallback صامت لـstaging عند غياب `VITE_CUSTOMER_API_ORIGIN`.
11. تحديث `README.md` الجذري و`print-agent/README.md` ليعكسا البنية الفعلية الحالية، وإنشاء `docs/DEPLOYMENT.md` مركزي يجمع تعليمات بناء/نشر كل الأهداف.
