# إصلاح نظام الترجمة — تقرير التغييرات

## المشكلة

عند تحويل الواجهة إلى الإنجليزية أو الأردو كانت أجزاء كبيرة تبقى بالعربية. تبيّن أن السبب **أربع علل منفصلة**، لا علة واحدة:

| # | العلة | الحجم |
|---|---|---|
| 1 | نصوص عربية مكتوبة مباشرة في الكود لا تمر بـ `t()` | ~3,637 سطراً في 134 ملفاً |
| 2 | مفاتيح `t()` مستدعاة في الكود وليس لها أي قيمة في أي ملف لغة | **647 مفتاحاً** |
| 3 | `ur.json` ناقص، و `fallbackLng: 'ar'` يعيد الأردو إلى العربية | 187 مفتاحاً |
| 4 | منطق الاتجاه يفحص `language === 'ar'` فقط، فالأردو تُعرض LTR | 102 موضعاً في 35 ملفاً |

العلة رقم 2 هي الأخطر ولم تكن ظاهرة: `t('orders.foo', 'نص عربي')` — إذا لم يوجد المفتاح، تُعرض القيمة الاحتياطية العربية **في كل اللغات، دائماً**.

---

## ما تم إصلاحه

### 1. ملفات اللغة

| | قبل | بعد |
|---|---|---|
| ar.json | 1,548 | **3,625** |
| en.json | 1,548 | **3,625** |
| ur.json | 1,361 | **3,625** |

- تطابق تام: صفر مفاتيح مفقودة بين اللغات الثلاث، صفر قيم فارغة.
- كل مفتاح `t()` في الكود (3,083 مفتاحاً) يُحلّ الآن بنجاح.
- صفر قيمة تحتوي `${...}` (i18next يستخدم `{{ }}`؛ الصيغة الأخرى كانت تُطبع حرفياً).

### 2. `src/lib/direction.ts` — ملف جديد

مصدر واحد لكل قرارات الاتجاه واللغة:

```ts
isRtlLang(lang)   // الأردو الآن RTL — كانت LTR
dirOf(lang)       // 'rtl' | 'ltr'
localeOf(lang)    // ar-SA / ur-PK / en-US بأرقام لاتينية
fontFamilyOf(lang)// Tajawal للعربية، Noto Nastaliq للأردو، Inter للإنجليزية
useDirection()    // خطاف: { t, lang, isRtl, dir, locale, pick }
changeAppLanguage(lang) // يبدّل اللغة + يحفظها + يزامن dir/lang/الخط
```

### 3. `src/i18n/config.ts`

- `fallbackLng` صار متسلسلاً: **الأردو ← الإنجليزية ← العربية** (كان يسقط على العربية مباشرة).
- `load: 'languageOnly'` + `supportedLngs` — يمنع فشل `en-US` في إيجاد حزمة `en`.
- مستمع `languageChanged` يزامن `<html dir/lang>` والخط مركزياً، فلم تعد المكوّنات تعدّل الـ DOM يدوياً.
- تحذير في وضع التطوير عند أي مفتاح ناقص، حتى لا تتكرر العلة رقم 2 صامتة.

### 4. الخطوط

الأردو لا تدعمها خطوط Tajawal. أُضيف `Noto Nastaliq Urdu` + `Noto Naskh Arabic` + `Inter`، مع `line-height` أكبر للأردو (خط النستعليق يحتاج ارتفاعاً وإلا انقطعت النزلات).

### 5. أخطاء منطقية اكتُشفت أثناء العمل وأُصلحت

- **`Staff.tsx` / `RolePermissionsSettings.tsx`**: كانا يبحثان عن تصنيف الصلاحية بمطابقة **نص عربي** (`'الطلبات' → 'orders'`). بعد أن صار `category` مترجَماً، انكسر البحث في الإنجليزية والأردو صامتاً. حُوّلا إلى `categoryKey` الثابت.
- **`saas.notif_trial_expiring_*` / `notif_subscription_expiring_*`**: كانت قيمهما متبادلة (نص التجربة تحت مفتاح الاشتراك والعكس).
- **جمع التكسير**: `d === 1 ? 'يوم' : 'أيام'` مكتوب يدوياً — استُبدل بصيغ i18next (`_zero/_one/_two/_few/_many/_other`)، لأن العربية لها ست فئات جمع والأردو اثنتان.
- **تنسيق التواريخ**: عدة شاشات كانت تستدعي `toLocaleDateString('ar-SA-u-nu-latn')` بشكل مثبّت، فيبقى التاريخ عربياً/هجرياً في الواجهة الإنجليزية. حُوّلت إلى `localeOf()`.
- **ملخّص الذكاء الاصطناعي**: كان الأمر الموجّه للنموذج ينص «لخّص بالعربية» دائماً. صار يتبع لغة الواجهة.

---

## ما تُرك عربياً عن قصد

| الفئة | العدد | السبب |
|---|---|---|
| تعليقات في الكود | 746 | ليست نصوص واجهة |
| قيم احتياطية ميتة `t('key','عربي')` | 603 | المفاتيح كلها موجودة الآن، فالقيم لا تُستخدم. تركها يقلّل حجم التعديل |
| قوالب ZATCA ثنائية اللغة | 186 | إلزام نظامي سعودي: عربي + إنجليزي جنباً إلى جنب |
| بيانات تُكتب في قاعدة البيانات | 88 | سجلات التدقيق وحقول الطلبات — ترجمتها تجعل السجل يتغير بتغير لغة الموظف |
| رسائل `console` | 31 | تشخيصية |
| أسماء اللغات وأنماط الأرقام | 9 | «العربية»/«اردو» تُكتب بخطها دائماً |

---

## التحقق

- `tsc --noEmit` — صفر أخطاء
- `vite build` — ينجح
- تطابق المفاتيح بين اللغات الثلاث — صفر فروق
- كل `t()` في الكود يُحلّ — صفر مفقود
- تطابق الرموز `{{...}}` بين اللغات — صفر اختلاف (عدا 4 مواضع جمع مثنى عربي، وهي صحيحة)
- اختبار تشغيل فعلي عبر i18next للغات الثلاث:

```
[ar] dir=rtl :: حفظ | إلغاء | العملاء | الإعدادات
[en] dir=ltr :: Save | Cancel | Customers | Settings
[ur] dir=rtl :: محفوظ کریں | منسوخ کریں | گاہک | ترتیبات
```

- فحص شامل: **قيمة إنجليزية واحدة** فقط تحتوي حرفاً عربياً، وهي مقصودة (شرح زر تبديل اللغة الذي يعرض «عربي / English / اردو»).

---

## ملاحظات للمتابعة

1. **الـ 603 قيمة احتياطية الميتة** يمكن إزالتها بمسحة ميكانيكية واحدة إن أردت صفر عربية في الكود.
2. `WhatsAppSettings.tsx` ما زال يستورد `DEFAULT_WHATSAPP_TEMPLATE`؛ يُفضّل التحويل إلى `getDefaultWhatsAppTemplate()` ليتبع اللغة.
3. `printRelay.ts` يعمل على Node وليس في المتصفح، فلا يستطيع استخدام i18next الحالي. رسائل أخطائه تصل للكاشير بالعربية. الحل المقترح: أن يُرجع `code` ثابتاً إلى جانب النص، وتُترجم في `PrinterSettings.tsx`.
4. `BillingSettings.tsx` يحفظ `plan_name` مترجَماً في قاعدة البيانات — يُفضّل حفظ المعرّف لا الاسم المعروض.
