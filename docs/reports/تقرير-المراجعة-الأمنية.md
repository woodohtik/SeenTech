# تقرير المراجعة الأمنية — نظام سين (Seen POS)

**التاريخ:** 11 أغسطس 2026
**النطاق:** المشروع بالكامل — الواجهة (React/Vite)، الخادم (Express)، قاعدة البيانات (Supabase/Postgres)، قواعد Firestore، والاعتماديات.
**الحالة:** تم تطبيق الإصلاحات على ملفات المشروع مباشرة، والتحقق منها ببناء قاعدة بيانات Postgres حقيقية واختبار العزل عملياً.

---

## ⚠️ أربع خطوات يدوية عاجلة (لا يمكن للكود القيام بها نيابةً عنك)

| # | الإجراء | لماذا الآن |
|---|---|---|
| 1 | **ألغِ مفتاح خدمة Firebase فوراً** من Google Cloud Console ← IAM ← Service Accounts ← Keys، ثم أنشئ مفتاحاً جديداً واحقنه عبر متغيّر البيئة `FIREBASE_SERVICE_ACCOUNT`. | المفتاح الخاص الكامل كان في جذر المشروع (`firebase-credentials.json`) وغير مستثنى في `.gitignore`. من يملكه يستطيع إصدار رمز دخول لأي حساب — بما فيه السوبر أدمن. **نقلته إلى `_to_delete/firebase-credentials.json.LEAKED`، لكن المفتاح محروق ولا ينفع تغيير مكانه.** إن سبق رفعه إلى Git فامسحه من التاريخ بـ `git filter-repo --path firebase-credentials.json --invert-paths`. |
| 2 | **شغّل `SECURITY_FIX.sql`** في Supabase SQL Editor (آخر ملف بعد بقية الهجرات). | يزيل سياسة `allow_all` التي كانت تمنح `anon` قراءة وكتابة وحذف **كل** جداول **كل** المستأجرين. |
| 3 | **انشر `firestore.rules` المحدّثة**: `firebase deploy --only firestore:rules`. | القواعد الحالية تسمح بالاستيلاء على أي مستأجر (تفاصيل في F-2 أدناه). |
| 4 | **دوّر مفتاح `GEMINI_API_KEY`.** | كان يُحقن حرفياً داخل حزمة الجافاسكربت المنشورة عبر `vite.config.ts`، أي أنه عام بالفعل لدى كل زائر. |

---

## ملخّص تنفيذي

| الخطورة | العدد | الحالة |
|---|---|---|
| 🔴 حرجة | 8 | أُصلحت في الكود · اثنتان تحتاجان الخطوتين 1 و2 أعلاه |
| 🟠 عالية | 9 | أُصلحت |
| 🟡 متوسطة | 12 | أُصلحت |
| 🔵 منخفضة | 6 | أُصلحت أو وُثّقت |

الخيط المشترك بين أخطر الثغرات: **قرارات أمنية تُتخذ في المتصفح ولا يُعاد التحقق منها في الخادم**، وقاعدة بيانات كانت سياساتها مفتوحة بالكامل فلم يبقَ خلف الواجهة أي خط دفاع ثانٍ.

---

## 🔴 الثغرات الحرجة

### C-1 · مفتاح خدمة Firebase مكشوف في المشروع
`firebase-credentials.json` — مفتاح خاص كامل لحساب `firebase-adminsdk-fbsvc@ai-studio-applet-webapp-70fe5`، و`.gitignore` كان يستثني `firebase-applet-config.json` فقط. في وضع التطوير كان `server.ts` يقدّم جذر المشروع عبر Vite، أي أن `GET /firebase-credentials.json` كان يعيده مباشرةً.

**ما فُعل:** نُقل إلى `_to_delete/`، وأُضيفت الأنماط `firebase-credentials.json` و`*service-account*.json` و`*-credentials.json` و`*.pem` و`*.p12` إلى `.gitignore`.
**ما تبقّى عليك:** إلغاء المفتاح (الخطوة 1).

### C-2 · تجاوز كامل للمصادقة: الخادم كان يقبل رموز JWT غير موقّعة
`src/server/middleware/authMiddleware.ts` — عند فشل `verifyIdToken` (توقيع خاطئ، انتهاء صلاحية، أو غياب مفاتيح Firebase Admin) كان الكود يفكّ تشفير جسم الرمز بـ base64 **دون التحقق من التوقيع** ويثق بحقلي `sub` و`email`. وبما أن السطر 120 كان يمنح `super_admin` لعنوان بريد ثابت، فإن هذا الطلب كان كافياً:

```bash
curl -H "Authorization: Bearer <header>.<payload يحوي بريد السوبر أدمن>.xxx" https://host/api/admin/stats
```

**ما فُعل:** حُذف مسار fallback بالكامل. الآن: إن لم تُهيّأ Firebase Admin يُرجع الخادم `503`، وإن فشل التحقق يُرجع `401`. أُضيف `verifyIdToken(idToken, true)` لرفض رموز الحسابات المعطّلة أو الملغاة. وحُذف باب البريد الثابت — السوبر أدمن يجب أن يكون صفاً فعّالاً في `saas_users`.

### C-3 · صفحة الدخول كانت تُنشئ حساب السوبر أدمن بكلمة مرور يختارها المهاجم
`src/components/Login.tsx` — عند فشل تسجيل الدخول ببريد السوبر أدمن بخطأ `auth/user-not-found` أو `auth/invalid-credential`، كان الكود يستدعي `createUserWithEmailAndPassword`. أي زائر مجهول يفتح `/login`، يُدخل ذلك البريد وأي كلمة مرور، فيصبح السوبر أدمن — ثم يقوم `App.tsx` بترقيته تلقائياً في `saas_users` فيتجاوز RLS على كل الجداول.

كما كان أي «رقم جوال» يحوي السلسلة `2566512` يُعيد توجيه الدخول صامتاً إلى حساب السوبر أدمن — أي المُدخل المطلوب بالضبط لتشغيل الثغرة.

**ما فُعل:** حُذف الإنشاء التلقائي وحُذف إعادة التوجيه بالسلسلة، وأصبحت رسالة الفشل موحّدة (`login.errors.invalid_credentials`) لإغلاق قناة تعداد الحسابات.

### C-4 · `allow-all-rls.sql` — إلغاء عزل المستأجرين بالكامل
```sql
CREATE POLICY allow_all ON <كل جدول> FOR ALL USING (true) WITH CHECK (true);
```
بدون عبارة `TO` تُمنح السياسة لدور `PUBLIC` — ويشمل `anon`. والسياسات المسموحة تُجمع بـ **OR**، فسياسة واحدة ألغت كل سياسات العزل في المشروع. ومفتاح `VITE_SUPABASE_ANON_KEY` عام بطبيعته داخل حزمة الجافاسكربت:

```bash
curl -H "apikey: <anon>" "$URL/rest/v1/customers?select=*"   # عملاء كل المتاجر
curl -X DELETE -H "apikey: <anon>" "$URL/rest/v1/orders?tenant_id=eq.<منافس>"
```
وهذا يشمل `staff.pin_hash` و`withdrawal_requests.iban` و`customers.measurements`.

**ما فُعل:** استُبدل محتوى `allow-all-rls.sql` بـ `RAISE EXCEPTION` حتى لا يُنفَّذ بالخطأ، وأُنشئ `SECURITY_FIX.sql` الذي يزيل السياسة ويعيد بناء العزل. **مهم:** الملف الجديد لا يترك أي جدول بلا سياسة — كل جدول يفقد `allow_all` ولا توجد له سياسة أخرى يحصل تلقائياً على عزل مناسب حسب أعمدته (`tenant_id` أو `uid`/`user_id` أو سوبر أدمن فقط مع تنبيه في السجل).

### C-5 · أي موظف كان يستطيع ترقية نفسه إلى «صاحب حساب»
سياسات المستأجرين في المخطط تتحقق من `tenant_id` فقط — **ولا تعرف الأدوار إطلاقاً**. أي خيّاط أو كاشير كان يستطيع من devtools:
```js
supabase.from('staff').update({ role:'owner' }).eq('uid','<uid الخاص به>')
```
وعند إعادة التحميل يقرأ `App.tsx` الدور من ذلك الصف ويمنحه كل الصلاحيات. الأمر نفسه ينطبق على جداول `roles` و`roles_permissions`.

**ما فُعل:** أُضيف مشغّل `staff_no_self_escalation` في قاعدة البيانات يمنع تعديل `role`/`role_id`/`status`/`tenant_id` على صف المستخدم نفسه، ويشترط أن يكون منفّذ أي تغيير دور `owner` أو `admin`. وأُضيفت سياسات كتابة على جداول الأدوار مقصورة على `owner`/`admin` مع إبقاء القراءة لأعضاء المستأجر (بما فيها الأدوار العامة `tenant_id IS NULL`).

**تم اختباره:** محاولة كاشير ترقية نفسه ترجع الآن `ERROR: لا يمكن تعديل الدور أو الحالة على حسابك الشخصي`.

### C-6 · هاشات PIN لكل الموظفين تُرسَل إلى كل متصفح
`PinLogin.tsx` و`LockScreen.tsx` و`staffService.ts` تنفّذ `select('*')` على `staff` — بما فيه `pin_hash` — ثم تقارن بـ `bcrypt` **في المتصفح**. مع مفتاح من 4 أرقام (10,000 احتمال) و`bcrypt` بتكلفة 10 وبلا أي عدّاد محاولات، يستطيع أي موظف نسخ هاش المدير من تبويب الشبكة وكسره دون اتصال خلال دقائق. كما كانت الأرقام تُولَّد بـ `Math.random()` — وهو متوقَّع من حالة V8.

**ما فُعل جزئياً:**
- التوليد أصبح بـ `crypto.getRandomValues` مع رفض العيّنات المنحازة (توزيع منتظم).
- أُضيف `src/utils/pinThrottle.ts`: قفل بعد 5 محاولات مع تصاعد أُسّي (دقيقة، دقيقتان، … بحد أقصى 15 دقيقة)، مربوطاً بكلا الشاشتين.

**ما تبقّى (يتطلّب قرارك):** الحل الكامل هو نقل المقارنة إلى الخادم ومنع قراءة العمود. الدالة جاهزة ومكتوبة في نهاية `SECURITY_FIX.sql` (`verify_staff_pin`) لكنها **معلّقة كتعليق عمداً**، لأن تفعيلها يتطلّب تعديل `PinLogin.tsx` و`LockScreen.tsx` أولاً وإلا تعطّل الدخول بالـ PIN. أخبرني إن أردت تنفيذ هذه الخطوة.

### C-7 · قواعد Firestore: استيلاء كامل على أي مستأجر
`firestore.rules` — قاعدة إنشاء `staff` كانت تقبل الفرع:
```
(request.auth.uid == staffId && request.resource.data.email == request.auth.token.email)
```
أي أن أي مستخدم يسجّل حساباً ثم يكتب `staff/<uid الخاص به>` بمحتوى `{role:'owner', tenantId:'<مستأجر الضحية>'}` — فتصبح `isOwner(victimTenant)` صحيحة، ويحصل على قراءة وكتابة كاملة على طلبات الضحية وعملائه ومخزونه وفواتيره.

كذلك كانت `hasPermission(permission)` **لا تقارن مستأجر المستخدم بمستأجر المستند إطلاقاً**. وبما أن كل قاعدة مكتوبة على شكل `(A && B) || C`، فإن كاشير في المتجر «أ» يملك `customers.view` كان يقرأ عملاء **كل** المتاجر.

**ما فُعل:**
- حُذف فرع الإنشاء الذاتي.
- استُبدلت `hasPermission(p)` بـ `hasPermissionIn(p, tenantId)` التي تشترط تطابق المستأجر وحالة `active`، وهُوجرت **29 موضع استدعاء**. النسخة القديمة أُبقيت لكن قُصرت على السوبر أدمن فقط، فلا يمكن لأي موضع منسيّ أن يمنح أكثر مما كان.
- تحديث الموظف لنفسه أصبح يثبّت `roleId` و`status` و`branchId` و`uid` (كان يثبّت `role` و`tenantId` فقط — و`roleId` هو الحقل الذي تقرأه دالة الصلاحيات فعلياً).
- `user_permission_overrides` أصبحت تتحقق من المستأجر الحقيقي لصاحب الصف، لا من الحقل الموجود داخل المستند المكتوب.
- `error_logs` لم تعد تقبل `allow create: if true`، و`saas_security_logs` تشترط أن يطابق `userId` هوية المستدعي.
- `isSuperAdmin()` أصبحت تدعم claim مخصّص (`request.auth.token.role`) بدل الاعتماد على بريد ثابت منشور في ملف قواعد يمكن لأي أحد قراءته.

### C-8 · برنامج الإحالة: سكّ رصيد وصرفه دون أي تحقق
سلسلة كاملة لسحب أموال:
1. `record_referral` كانت **ممنوحة لـ `anon`** وتكتب في جدول `tenants` لأي مستأجر — أي أن مجهولاً يستطيع أن يجعل نفسه «مُحيلاً» لكل قاعدة عملائك.
2. `confirm_referral` كانت قابلة للتنفيذ من `PUBLIC` (المنح الافتراضي في Postgres) وتضيف 300 ر.س دون التحقق من وجود اشتراك مدفوع.
3. `request_withdrawal` كانت تأخذ `p_tenant` **من العميل** — أي سحب رصيد محفظة أي متجر آخر إلى IBAN المهاجم.
4. `process_withdrawal` كانت تعتمد الصرف **بلا أي تحقق من صلاحية المنفّذ**.

**ما فُعل:** أُعيدت كتابة الدوال الأربع بتحقق داخلي (ملكية المستأجر / سوبر أدمن)، وحُذف `p_tenant` من `request_withdrawal` (تشتقّه الآن من الـ JWT) **مع تعديل `src/services/referralService.ts` ليطابق التوقيع الجديد**، وسُحبت صلاحية التنفيذ من `anon` و`PUBLIC`.

---

## 🟠 الثغرات العالية

### H-1 · `/api/public/invoices/:id` — تسريب بيانات بلا مصادقة
نقطة غير محمية تستخدم مفتاح `service_role` (يتجاوز RLS بالكامل) وتنفّذ `select('*')` ثم تنشر الصفوف كاملةً. من يملك رقم فاتورة واحد (يصل عبر رابط WhatsApp أو سجل المتصفح) كان يحصل على:
- من `orders`: `notes`، `images` (صور ملابس العميل)، `created_by`، `branch_id`
- من `tenants`: `owner_email`، `phone`، `commercial_register`، `owner_uid`
- من `customers`: الجوال، البريد، الملاحظات، و**المقاسات الجسدية**

**ما فُعل:** التحقق من صيغة UUID، ثم بناء الرد من **قائمة سماح صريحة** للحقول التي تعرضها الفاتورة فعلاً فقط. من العميل يعود الاسم فقط. (أبقيت `select('*')` عمداً وفلترت الرد، حتى لا يتعطّل شيء إن اختلفت أسماء الأعمدة في قاعدتك عن ملف المخطط.)

### H-2 · SSRF ومسح منافذ الشبكة الداخلية بلا مصادقة
`/api/print/raw` و`/api/print/probe` — نقطتان مفتوحتان تجعلان **الخادم** يفتح اتصال TCP إلى `host:port` يحدّده المتصل ويكتب بايتات يحدّدها المتصل. وفلتر `isPrivateHost` معكوس: يسمح بالضبط بالعناوين التي يستهدفها SSRF (`127.x`، `10/8`، `172.16/12`، `192.168/16`).

**ما فُعل:** الطباعة المباشرة أصبحت **معطّلة افتراضياً** ولا تعمل إلا بـ `ENABLE_DIRECT_LAN_PRINT=true` (للخوادم داخل شبكة المتجر فقط)؛ ومُنعت عناوين loopback؛ وقُصرت المنافذ على `9100-9103, 515, 631`؛ وأصبحت رسائل الفشل موحّدة (كانت تميّز بين «مرفوض» و«مهلة» فتعمل كأداة مسح دقيقة)؛ وأُضيف حدّ معدل.

### H-3 · تخمين رمز اقتران الطابعة
عدّاد المحاولات الوحيد كان مبنياً على ترويسة `X-Forwarded-For` التي يتحكم بها العميل — تغييرها كل طلب يصفّر العدّاد. عند الإصابة يسلّم الخادم `clientToken` لمحطة متجر آخر: طباعة إيصالات مزوّرة وفتح درج النقد.

**ما فُعل:** `app.set('trust proxy', 1)`، وعدّاد لكل **رمز** (قفل بعد 5)، وميزانية عامة (120/دقيقة)، ورمز الاقتران أصبح **يُستهلك مرة واحدة** — مع توليد رمز جديد عند إعادة تشغيل الوسيط حتى لا يتعطّل الاقتران من جهاز بديل.

### H-4 · `impersonatedTenantId` في localStorage يمنح صلاحيات «صاحب حساب»
`AuthContext.tsx` كان يقرأ القيمة من localStorage عند الإنشاء، و`App.tsx` يستهلكها **بلا أي تحقق من أن المستخدم سوبر أدمن**، و`ProtectedRoute` يتجاوز كل الحراسات عند وجودها. التحقق الوحيد كان داخل `try` لا يُنظّف في `catch` — أي أن حظر طلب شبكة واحد من devtools كان يثبّت واجهة المالك طوال الجلسة.

**ما فُعل:** الحالة تبدأ `null` دائماً، ولا تُستعاد إلا بعد تأكيد `role === 'super_admin'` من قاعدة البيانات، ويُنظَّف في `catch`. وأُضيفت حراسة ثانية في `App.tsx`.

### H-5 · حالة الدخول كانت تُبنى من localStorage
`App.tsx` كان يهيّئ `userRole` و`tenantId` و`isApproved` من localStorage، ومسار `/admin/*` كان ينقصه شرط `user &&` (موجود في كل مسار آخر)، و`catch` فشل التحقق كان يبقي `...prev`. النتيجة: `localStorage.setItem('user_role','super_admin')` + حظر طلب واحد = بوابة إدارة SaaS كاملة.

**ما فُعل:** الحالة الابتدائية أصبحت خارج نطاق تحكّم المستخدم، وأُضيف `user &&`، و`catch` يعيد التعيين إلى حالة «غير مسجّل».

### H-6 · الصلاحيات الفعّالة كانت تُقرأ من ذاكرة localStorage
`permissionService.ts` كان يقرأ `role_permissions_<tenant>_<role>` من localStorage. وللمستأجرين الذين لم يخصّصوا أدواراً (الحالة الافتراضية) لا يوجد صف في قاعدة البيانات يستبدلها — فكانت هذه القيمة **هي** خريطة الصلاحيات الفعلية. سطر واحد في devtools يمنح `system.delete`.

**ما فُعل:** حُذف الاعتماد على الذاكرة المحلية كمصدر تفويض.

### H-7 · `GEMINI_API_KEY` داخل حزمة المتصفح
`vite.config.ts` كان يستخدم `define` لحقن المفتاح نصياً في كل ملف JS منشور — رغم أن `.env.example` ينص صراحةً على إبقائه في الخادم.
**ما فُعل:** حُذف `define`. الميزة تتعطّل بلطف عند غياب المفتاح (الكود محمي بـ `typeof process !== 'undefined'`). **دوّر المفتاح** — هو عام بالفعل.

### H-8 · رفع SVG ⇒ XSS مخزّن
التحقق كان `file.type.startsWith('image/')` — و`file.type` تخمين المتصفح ويتحكم به المهاجم. `image/svg+xml` يمرّ، ويُخزَّن نوع المحتوى كما هو في مخزن **عام القراءة**. من يفتح رابط «الشعار» ينفّذ سكربتاً على نطاق التخزين.
**ما فُعل:** فحص **البايتات السحرية** الفعلية (JPEG/PNG/GIF/WebP فقط)، واشتقاق الامتداد ونوع المحتوى مما اكتُشف لا مما أرسله العميل، وحدّ حجم 8 ميغابايت، وتأمين مسار المجلد.

### H-9 · `verifySaaSStaff` كانت تعتبر `gmail.com` نطاقاً رسمياً
أي عنوان Gmail كان يمرّ فحص «البريد الرسمي»، كما أن `split('@')[1]` تعطي النتيجة الخاطئة لـ `a@b@evil.com`.
**ما فُعل:** حُذف `gmail.com`، ويُقرأ النطاق بعد آخر `@`.

---

## 🟡 الثغرات المتوسطة (كلها أُصلحت)

| # | الثغرة | الإصلاح |
|---|---|---|
| M-1 | لا CSP ولا HSTS ولا X-Frame-Options ولا nosniff | ترويسات أمنية في `server.ts` و`vercel.json` (مع السماح صراحةً لنطاقات Firebase Auth وMoyasar والخطوط حتى لا يتعطّل الدخول أو الدفع) |
| M-2 | لا تحديد معدل على أي مسار | محدِّد معدل داخلي بلا اعتماديات جديدة، بأسقف مناسبة لمتجر خلف NAT واحد |
| M-3 | لا قيد على أصل الطلبات (CORS) | قائمة سماح من `APP_URL` مع تطبيع الأصل والسماح دائماً بنفس الأصل |
| M-4 | حدّ جسم الطلب 25 ميغابايت على **كل** المسارات بما فيها غير المحمية | 256 كيلوبايت افتراضياً، و12 ميغابايت لمساري الطباعة فقط |
| M-5 | شاشة القفل تفتح بأي 4 أرقام عند غياب سياق الموظف | فشل مغلق + عدّاد محاولات |
| M-6 | `saas_settings` مقروء للجميع (يشمل `temp_passwords`) | سياسات مفاتيح محدّدة + قراءة إدارية |
| M-7 | `saas_security_logs` يقبل الإدراج من `anon` ⇒ تزوير السجل الجنائي | يشترط تطابق `user_id` مع هوية المستدعي |
| M-8 | معرّف الجلسة بـ `Math.random()` | `crypto.randomUUID()` |
| M-9 | رمز الاقتران يُكتب في سجلات الخادم | يُسجَّل معرّف مختصر بدلاً منه |
| M-10 | رسائل الخطأ الداخلية تُعاد للعميل (تكشف أسماء الجداول والأعمدة) | رسائل عامة |
| M-11 | المورّد يستطيع منح نفسه شارة «موثّق من سين» | فصل السياسات + `REVOKE UPDATE (is_verified)` |
| M-12 | نموذج الهبوط يقبل أي إدراج من `anon` ⇒ حرمان منافس من التجربة المجانية | تحقق من الشكل + سياسة منفصلة للمستخدمين المسجّلين |

---

## 🔵 نقاط لم أغيّرها — تحتاج قرارك

1. **`xlsx@0.18.5`** — تحذيران بخطورة عالية: تلويث Prototype (GHSA-4r6h-8v6p-xvw6) وReDoS (GHSA-5pgg-2g8v-p4x9). لا يوجد إصلاح عبر npm لأن SheetJS توقّفت عن النشر هناك عند 0.18.5. الحل: تغيير الاعتمادية إلى `"xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"` أو الانتقال إلى `exceljs`. لم أطبّقه لأنه يغيّر طريقة حلّ الاعتماديات وقد يحتاج تعديلات في الاستيراد.

2. **لا يوجد ملف قفل اعتماديات** (`package-lock.json` غير مثبّت في المشروع). كل الحزم تُحلّ عبر نطاقات `^`، أي أن إصدار patch مخترَق لأي حزمة فرعية يصل للإنتاج صامتاً. ثبّت ملف قفل واستخدم `npm ci`.

3. **المصادقة الثنائية غير مطبّقة إطلاقاً.** `sessionStorage.saas_2fa_verified` يُكتب عند الدخول ولا يُقرأ في أي مكان، و`is2FAVerified = true` ثابت، ولا توجد سياسة تشير إليه. علّقت الكود بوضوح حتى لا يُظن أنه ضابط أمني، لكن التطبيق الفعلي (Firebase MFA + claim يتحقق منه الخادم) قرار منتج.

4. **التحقق من PIN في الخادم** — انظر C-6.

---

## الملفات التي عُدّلت

**كود** (19 ملفاً): `server.ts` · `vite.config.ts` · `vercel.json` · `firestore.rules` · `.gitignore` · `.env.example` · `src/App.tsx` · `src/contexts/AuthContext.tsx` · `src/components/{Login,PinLogin,LockScreen,ImageUpload}.tsx` · `src/components/Inventory/ProductImageUploader.tsx` · `src/server/{firebase-admin,printRelay}.ts` · `src/server/middleware/authMiddleware.ts` · `src/services/{permissionService,staffService,saasSecurityService,referralService}.ts` · `src/lib/supabase/storage.ts` · `src/utils/session.ts` · ملفات الترجمة الثلاثة

**ملفات جديدة:** `SECURITY_FIX.sql` · `src/utils/pinThrottle.ts`

**عُطِّل:** `allow-all-rls.sql` (يرفع استثناءً الآن) · `firebase-credentials.json` (نُقل إلى `_to_delete/`)

كل تعديل مسبوق بتعليق يبدأ بـ `SEC-` أو `SEC-F` يشرح ما كانت المشكلة — للمراجعة أو التراجع.

---

## كيف تحقّقت من الإصلاحات

1. **تحليل نحوي** لكل ملفات TypeScript المعدّلة عبر esbuild — نجحت جميعها.
2. **مراجعة انحدار مستقلة** كشفت 13 مشكلة في الإصلاحات نفسها (فاصلة منقوطة ناقصة كانت ستُفشل نشر قواعد Firestore بالكامل، وتوقيع RPC لا يطابق العميل، وCSP يحجب دخول Google، وسياسة تمنع قراءة الأدوار العامة) — أُصلحت كلها.
3. **قاعدة بيانات Postgres 16 حقيقية**: بنيت المخطط من ملفاتك، حاكيت الحالة المصابة (56 سياسة `allow_all`)، ثم شغّلت `SECURITY_FIX.sql`:

| الاختبار | النتيجة |
|---|---|
| سياسات `allow_all` | 56 ← **0** |
| جداول بلا أي سياسة (كانت ستتعطّل) | **0** |
| `anon` يقرأ `customers` / `staff` / `saas_settings` | **0 صف** |
| `anon` يقرأ `plans` (صفحة الأسعار) | **يعمل** — الفعّالة فقط |
| `anon` يُدرج عميلاً محتملاً من صفحة الهبوط | **يعمل** |
| كاشير يقرأ عملاء مستأجره | **يعمل** |
| كاشير يقرأ عملاء مستأجر آخر | **0 صف** |
| كاشير يرقّي نفسه إلى `owner` | **مرفوض** |
| كاشير ينشئ عميلاً في مستأجر آخر | **مرفوض** |
| كاشير يستدعي `process_withdrawal` | **مرفوض** |
| `anon` يستدعي `record_referral` | **مرفوض** |
| إعادة تشغيل `SECURITY_FIX.sql` ثلاث مرات | **بلا أخطاء** (idempotent) |
| `allow-all-rls.sql` | **يرفض التنفيذ** |

---

## ترتيب التنفيذ المقترح

1. ألغِ مفتاح Firebase (الخطوة 1) — كل شيء آخر ثانوي أمامه.
2. انشر تعديلات الكود.
3. شغّل `SECURITY_FIX.sql` وراجع مخرجات `NOTICE` و`WARNING`: ستُخبرك بأي جدول قُصر على السوبر أدمن لأنه لا يحوي `tenant_id` — راجعه يدوياً. الاستعلامان في نهاية الملف يجب أن يعودا فارغين.
4. انشر `firestore.rules`.
5. دوّر `GEMINI_API_KEY`، واضبط `ENABLE_DIRECT_LAN_PRINT` و`APP_URL` في متغيّرات البيئة.
6. اختبر: تسجيل الدخول، الدخول بالـ PIN، إنشاء طلب، الطباعة، فتح فاتورة عامة، رفع صورة منتج.
