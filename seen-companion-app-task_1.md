# مهمة: تطبيق مرافق (Companion App) — تحديث حالة الطلب للموظف + تتبّع وإشعارات للعميل

## 🎭 الأدوار الأربعة المطلوبة معًا (بالترتيب الذي تُستخدم فيه)

1. **محلل (Analyst)** — يقرر أولاً *ما هو* التطبيق المناسب فعليًا لهذا المشروع تحديدًا (وليس افتراض "تطبيق جوال منفصل" تلقائيًا) بناءً على البنية التقنية الفعلية الموجودة — قسم "القرار المعماري" أدناه هو ناتج هذا الدور، اقرأه قبل أي تنفيذ.
2. **مدير مشروع (PM)** — يقسّم العمل لمراحل صغيرة قابلة للتسليم كل واحدة على حدة، بترتيب يقلّل المخاطر على نظام إنتاجي حي متعدد المستأجرين.
3. **مبرمج (Engineer)** — ينفّذ، مستعينًا إلزاميًا بـ Superpowers (`brainstorming` قبل كل مرحلة عالية الخطورة، `test-driven-development`، `systematic-debugging` عند أي عطل) و`frontend-design` لتصميم واجهتي التنبيهات، تمامًا كما حُدّد في `seen-superpowers-stage3-task.md` و`seen-dev-tools-integration-task.md`.
4. **مراجع (Reviewer)** — يشغّل `/code-review high --fix` على كل مرحلة قبل الانتقال للتالية (ليس في النهاية فقط)، لأن هذه الميزة تلمس المصادقة والإشعارات والبيانات العامة — سطح هجوم حقيقي.

**قاعدة صارمة**: لا تُنفَّذ مرحلتان معًا في نفس الجلسة دون تشغيل `npm run test` (Vitest، مثبّت من `seen-superpowers-stage3-task.md`) و`/code-review` بينهما.

---

## 🔍 القرار المعماري (تحليل — نتيجة فحص فعلي للكود، وليس افتراضًا)

فحصتُ الكود فعليًا قبل كتابة هذه المهمة. النتائج غيّرت شكل الحل جذريًا عمّا قد يُفترض ابتداءً:

### ما هو موجود بالفعل (لا تُعِد بناءه)
| العنصر | الحالة الفعلية |
|---|---|
| `src/hooks/useRealtimeSync.ts` | **جاهز وعامّ** — اشتراك Supabase Realtime معزول بالـ tenant، مستخدَم فعليًا في `POS.tsx`, `Orders.tsx` (تعديل الحالة) وغيرها. هذا يحل مشكلة "ظهور الطلبات الجديدة فورًا" على مستوى التبويب المفتوح — **لا حاجة لإعادة بنائه**. |
| `src/components/public/OrderTracking.tsx` | **موجود فعلاً** — صفحة تتبّع طلب للعميل، مربوطة بمسار `/track/:token` في `App.tsx` (سطر 543). لكنها **معطّلة فعليًا**: تستدعي دالة `supabase.rpc('get_public_order_tracking', ...)` **غير موجودة في أي migration** في `supabase/migrations/` ولا في جذر المشروع، ولا يوجد عمود `tracking_token` على جدول `orders` في أي مكان. الملف نفسه يشير إلى مواصفة `PUBLIC_TRACKING_SPEC.md` **غير موجودة أيضًا**. خلاصة: **هذه الميزة صُمّمت ولم تُكمَل** — واجهة بلا خلفية. |
| تكامل واتساب | **موسّع وفعلي بالفعل** (`src/utils/whatsapp.ts`, `WhatsAppSettings.tsx`, `WhatsAppPhoneModal.tsx`, ويُستخدم في `Orders.tsx`, `POS.tsx`, `TaxInvoices.tsx` وغيرها) — نمط "رابط wa.me جاهز بنص مُعبّأ". **استخدم نفس النمط** لإرسال رابط التتبّع، لا تبنِ آلية إرسال جديدة. |
| Firebase | مُهيّأ بالفعل في `src/lib/firebase.ts` لكن **للمصادقة (Auth) فقط** — `messagingSenderId` موجود في متغيرات البيئة لكن **لا يوجد أي استخدام لـ Firebase Cloud Messaging (FCM)** في كامل المشروع (تحقّق مباشر: صفر نتائج لـ `getMessaging`/`FCM`/`sendNotification`). |
| PWA / Push | **لا يوجد إطلاقًا** — لا `vite-plugin-pwa`، لا `manifest.json`، لا `service worker`، لا مفاتيح VAPID، لا مكتبة `web-push`. هذه هي الفجوة الحقيقية الوحيدة التي تحتاج بناءً من الصفر. |
| `print-agent/` | تطبيق Node.js منفصل صغير يعمل كوسيط بين المتصفح وطابعة محلية — **سابقة معمارية جيدة**: "تطبيق مرافق صغير يتحدث مع نفس الـ backend" نمط موجود ومُختبَر في هذا المشروع فعلاً. |

### القرار: PWA (تطبيق ويب تقدّمي) لا تطبيق جوال منفصل (React Native/Flutter)

**السبب (تحليل، لا تفضيل)**: بناء تطبيق منفصل بلغة/إطار مختلف يعني إعادة بناء طبقة المصادقة (Firebase Auth + نظام الأدوار الحالي) وطبقة البيانات (Supabase client) من الصفر في مشروع ثانٍ، وصيانة نسختين من كل منطق عمل. بينما المشروع الحالي (React + Vite + Supabase + Firebase) **يدعم PWA بإضافات صغيرة فقط**:
- تطبيق **قابل للتثبيت** على شاشة الجوال الرئيسية (أندرويد/iOS/ديسكتوب) بلا متجر تطبيقات.
- **إشعارات Push حقيقية** حتى لو التطبيق مغلقًا (عبر FCM + Service Worker) — بالضبط ما طُلب ("إشعارات وتنبيهات").
- إعادة استخدام 100% من كود المصادقة والصلاحيات والبيانات الموجود.
- ترقية لاحقة اختيارية لتطبيق متجر حقيقي عبر Capacitor (يغلّف نفس الكود) إن احتُجت مستقبلاً — قرار مؤجَّل، ليس الآن.

إن كان لديك سبب تجاري صريح يفرض تطبيق متجر رسمي (App Store/Play Store) أخبرني قبل التنفيذ — القرار أعلاه مبني على الأنسب هندسيًا لا على استحالة الخيار الآخر.

---

## 📋 خطة العمل (PM) — 6 مراحل، كل واحدة قابلة للتسليم منفردة

### المرحلة 0 — `brainstorming` (Superpowers، قبل أي كود)
أجب صراحة قبل البدء: (أ) من يُنشئ `tracking_token`؟ يجب أن يُولَّد عشوائيًا (لا رقم طلب متسلسل قابل للتخمين — كما نبّه تعليق الكود نفسه) عند إنشاء الطلب. (ب) أين يُخزَّن اشتراك الإشعارات؟ للموظف: مرتبط بـ `staff_id`. للعميل: **لا يوجد حساب للعميل** — يجب ربطه بـ `tracking_token` نفسه لا بهوية مستخدم. (ج) من يُطلق الإشعار عند تغيّر الحالة؟ الخادم (`server.ts`) عند أي تحديث لـ `orders.status`/`status_key`، لا العميل.

### المرحلة 1 — إكمال أساس تتبّع العميل (الأولوية الأولى، يُصلح ميزة معطّلة فعليًا)
1. Migration جديدة (`supabase/migrations/`، بنفس نمط ملفات التسمية بالتاريخ الموجودة): إضافة عمود `orders.tracking_token` (نص عشوائي فريد، مولَّد بـ `gen_random_uuid()` أو مكافئ، `UNIQUE`, مفهرَس)، وتوليده تلقائيًا لكل طلب جديد (trigger أو في نفس استعلام الإنشاء).
2. أنشئ دالة `get_public_order_tracking(p_token text)` بصلاحيات `SECURITY DEFINER` محدودة، تُرجع **فقط** الحقول التي يقرأها `OrderTracking.tsx` حاليًا (`order_number`, `status`, `shop_name`, `shop_logo_url`, `delivery_date`) — لا شيء آخر من جدول `orders`. أضف RLS/GRANT بحيث `anon` يستدعي هذه الدالة فقط، لا يقرأ الجدول مباشرة.
3. أضف Rate limiting على هذه الدالة تحديدًا (نفس منطق `pairAttemptsPerMinute` الموجود في `src/server/printRelay.ts`) لمنع تخمين التوكِنات بالقوة الغاشمة.
4. زر "إرسال رابط التتبّع" في `Orders.tsx` بجانب كل طلب: يبني رابط `https://<domain>/track/<tracking_token>` ويفتحه عبر **نفس نمط `src/utils/whatsapp.ts`** الموجود (لا مكتبة جديدة).
5. وثّق هذا كله في ملف `PUBLIC_TRACKING_SPEC.md` (الملف الذي أشار له الكود ولم يُكتب) — فيه شكل الـ RPC، صلاحياته، ومنطق الـ rate limit.

**اختبار (TDD)**: توكِن غير صحيح → لا بيانات لا خطأ يكشف بنية الجدول. توكِن صحيح → البيانات المسموحة فقط. تجاوز حد المحاولات → حظر مؤقت.

### المرحلة 2 — إشعارات Push للعميل (اختيارية من طرفه — Opt-in فعلي)
1. أضف `vite-plugin-pwa` + `manifest.json` بسيط (اسم "سِين"، أيقونة من `public/Logo.svg` الموجود).
2. في صفحة `/track/:token` تحديدًا: زر "فعّل إشعارات حالة الطلب" — عند الضغط يطلب إذن المتصفح، ثم عبر FCM (Firebase مُهيّأ أصلاً) يسجّل توكِن الجهاز مربوطًا بـ `tracking_token` (**ليس** بهوية مستخدم — العميل بلا حساب) في جدول جديد `order_push_subscriptions`.
3. في الخادم: أي تحديث لحالة الطلب (نقطة تحديث الحالة الموجودة في `Orders.tsx`/مسار الخادم المقابل) → إن وُجد اشتراك مرتبط بهذا الطلب → أرسل إشعار FCM مختصر ("طلبك رقم X أصبح: جاهز للاستلام").
4. لا تلمس منطق تحديث الحالة الحالي — فقط **أضف** استدعاء إرسال إشعار بعده (نمط إضافي/defensive، كما في `seen-fault-isolation-task.md`: فشل إرسال الإشعار لا يجب أن يفشل تحديث الحالة نفسه — لفّه بـ try/catch منفصل لا يُوقف الطلب).

### المرحلة 3 — لوحة "طلبات جديدة" + إشعارات للموظف
1. لا تبنِ شاشة تعديل حالة جديدة — **موجودة بالفعل** في `Orders.tsx`. المطلوب إضافي فقط: تنبيه فوري (صوت + Toast مرئي، استخدم `frontend-design` لتصميمه بلا "AI-slop") عند وصول طلب جديد **أثناء فتح التطبيق** — مبني فوق `useRealtimeSync('orders', tenantId, ...)` الموجود أصلاً، فقط أضف شرط `payload.eventType === 'INSERT'`.
2. لحالة **التطبيق مغلق/الجوال مقفل**: نفس آلية FCM من المرحلة 2، لكن الاشتراك هنا مرتبط بـ `staff_id` (الموظف له حساب فعلي، بخلاف العميل). عند INSERT جديد في `orders` لنفس الـ tenant → أرسل push لكل الموظفين المشتركين ذوي الصلاحية (`orders:view` من `src/constants/permissions.ts`).
3. اجعل تفعيل هذا الإشعار **اختياريًا من إعدادات الموظف** (`Settings.tsx` أو `StaffPinSetup.tsx` المناسب) — لا تفرضه صامتًا.

### المرحلة 4 — تغليف PWA وتثبيت
1. اختبر التثبيت الفعلي على: أندرويد (Chrome: "إضافة إلى الشاشة الرئيسية")، iOS (Safari: "Add to Home Screen" — قيود Apple على Push في PWA أقدم من iOS 16.4، تحقّق من الحد الأدنى المدعوم)، ديسكتوب.
2. تأكد أن التطبيق المثبّت يفتح مباشرة على شاشة تسجيل الدخول الحالية — **لا مسار مصادقة جديد**.

### المرحلة 5 — مراجعة واختبار شامل (Reviewer)
1. `/code-review high --fix` على كامل التغييرات.
2. أضف سيناريو Playwright (عبر `qa-skills` من `seen-dev-tools-integration-task.md`): عميل يفتح رابط تتبّع، يفعّل الإشعارات، موظف يغيّر حالة الطلب، العميل يستقبل الإشعار (محاكاة).
3. شغّل `/security-review` مرة أخرى على المسار العام الجديد تحديدًا (`get_public_order_tracking` + جدول `order_push_subscriptions`) — بيانات عامة بلا مصادقة = أعلى حساسية أمنية في هذه الميزة تحديدًا.

---

## ✅ معايير القبول (Definition of Done)

- [ ] `tracking_token` يُولَّد عشوائيًا لكل طلب جديد، وغير قابل للتخمين (تحقّق إنتروبيا التوكِن صراحة)
- [ ] `/track/:token` تعمل فعليًا (لم تكن كذلك قبل هذه المهمة) وتُرجع فقط الحقول المسموحة
- [ ] العميل يمكنه تفعيل/تعطيل الإشعارات من نفس صفحة التتبّع، دون أي حساب أو تسجيل دخول
- [ ] الموظف يرى تنبيه فوري (تبويب مفتوح) وإشعار Push (تطبيق مغلق، اختياري) عند طلب جديد
- [ ] فشل إرسال أي إشعار لا يوقف أو يبطئ تحديث حالة الطلب نفسه
- [ ] لا تغيير على منطق تعديل حالة الطلب الموجود في `Orders.tsx` — فقط إضافات
- [ ] `PUBLIC_TRACKING_SPEC.md` موجود ومكتمل
- [ ] `/code-review high` و`/security-review` نُفِّذا على كل مرحلة حساسة، والنتائج موثّقة
- [ ] اختبار تثبيت PWA فعلي ناجح على جهاز أندرويد أو iOS حقيقي واحد على الأقل (لا محاكي فقط)
