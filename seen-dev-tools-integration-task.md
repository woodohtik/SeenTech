# مهمة: تثبيت واستخدام Playwright + Frontend Design + Code Review + Refactor

## 🎭 الدور (Role)
تصرف كـ **Senior Full-Stack Engineer** يدير دورة تطوير كاملة: تصميم → اختبار آلي (E2E) → مراجعة كود → تحسين هيكلي (refactor) منضبط. نفّذ الأقسام بالترتيب أدناه — كل قسم مستقل ويمكن تشغيله في جلسة منفصلة إن لزم.

**ملاحظة تمهيدية مهمة**: قسمَا "Frontend Design" و"Playwright" أدناه هما إضافتان (plugins) رسميتان لهما أمر تثبيت مباشر (`/plugin install`) — يحتاجان اتصال إنترنت فعليًا من جلسة Claude Code التي تُثبَّت منها (وليس بيئة معزولة بلا إنترنت)، لذا نفّذهما من نفس جلسة Claude Code العادية التي تعمل بها على هذا المشروع.

---

## 1️⃣ Frontend Design (إضافة رسمية من Anthropic) — للتصميم التوليدي

### التثبيت
```
/plugin marketplace add anthropics/claude-code
/plugin install frontend-design@claude-code-plugins
```

### الفرق عن المهارات المثبّتة عندك مسبقًا
عندك بالفعل `ui-ux-pro-max` (بحث في كتالوج تصاميم/ألوان/خطوط جاهزة) و`design-system` (توكِنز تصميم/مكوّنات) و`ui-styling` (Tailwind + shadcn/ui). هذه الثلاثة **مرجعية** — تعطيك بيانات وأنماطًا جاهزة تستخدمها عند البناء.

`frontend-design` مختلف: هو **منهجية توليد تصميم من الصفر** بخطوات (Brainstorm → Plan → Critique → Build → Critique مرة ثانية)، وتحديدًا يحذّر من 3 أنماط "AI-slop" شائعة (تصاميم قوالب نمطية متكررة). الأفضل استخدامه **مع** الثلاثة الموجودة، لا بدلًا عنها.

### أول استخدام فعلي مقترح على هذا المشروع
طبّق `frontend-design` على **خطوة اختيار النشاط التجاري (vertical) في شاشة التسجيل** — وهي شاشة جديدة ستُبنى أصلًا ضمن "المرحلة 2" من `seen-activate-verticals-task.md` (اختيار من `mens_tailoring` إلى `specialty_clothing`). بدل بناء `<select>` بسيط، استخدم منهجية frontend-design لتصميم خطوة اختيار بصرية (بطاقات بأيقونات لكل نشاط بدل قائمة منسدلة نصية) — استعن أيضًا بـ`ui-ux-pro-max --domain landing` و`design-system` أثناء مرحلة Build.

---

## 2️⃣ Playwright / اختبارات E2E — عبر إضافة `qa-skills`

### لماذا هذه الإضافة تحديدًا
تم فحص عدة خيارات (`neonwatty/qa-skills`, `yusuftayman/playwright-cli-agents`, `lackeyjb/playwright-skill`). الأنسب لهذا المشروع تحديدًا هو **`neonwatty/qa-skills`** لسببين:
- تتضمن مولّد سيناريوهات "multi-user workflow" — يناسب بنية هذا المشروع متعددة الأدوار (سوبر أدمن / مالك / كاشير) تمامًا.
- تتضمن `mobile-ux-auditor` كوكيل تدقيق مستقل — يبني مباشرة على عمل تدقيق الاستجابة (Responsive) الذي أُنجز للتو في `seen-responsive-audit-task.md`.

### التثبيت
```
/plugin marketplace add neonwatty/qa-skills
/plugin install qa-skills@neonwatty-qa
```
يتطلب أيضًا مرة واحدة:
```
npm install -g @playwright/cli@latest
playwright-cli install
```

⚠️ **لم أتحقق بنفسي من كود هذه الإضافة سطرًا بسطر** (كما فعلت مع `ui-ux-pro-max` سابقًا عبر استنساخ وفحص السكربتات) — فقط من ملف README المنشور. إن كنت تفضّل تحققًا كاملاً قبل التثبيت أخبرني وأقوم به أولاً.

### أول استخدام فعلي مقترح على هذا المشروع
غطِّ بالضبط جدول التحقق الموجود مسبقًا في "المرحلة 4" من `seen-activate-verticals-task.md` (دورة حياة الطلب الكاملة لكل من الأنشطة الخمسة، من التسجيل حتى التسليم) — بدل تنفيذه يدويًا، ولّد سيناريوهات Playwright آلية له عبر مولّد "multi-user workflow" في qa-skills، بالإضافة إلى سيناريو تسجيل دخول لكل دور (سوبر أدمن/مالك/كاشير) على حدة.

---

## 3️⃣ Code Review — لا يحتاج أي تثبيت

هذا أمر **مدمج أصلاً** في Claude Code، لا إضافة خارجية له. الصيغة:
```
/code-review [target] [مستوى الجهد] [--fix] [--comment]
```
مستويات الجهد: `low` `medium` `high` `max` `ultra`. يصنّف الملاحظات: 🔴 مهم / 🟡 ملاحظة بسيطة / 🟣 موجود مسبقًا (ليس من هذا التغيير).

### أول استخدام فعلي مقترح
شغّل `/code-review high --fix` على مجموع التغييرات الأخيرة (المهام الثلاث المنفّذة مؤخرًا: تفعيل الأنشطة، عزل الأعطال، الاستجابة) **قبل** رفعها لأي بيئة إنتاج — هذه بالضبط الحالة التي صُمم لها: مراجعة قبل الدمج.

(الإضافة المنفصلة `anthropics/claude-code-security-review` أُنجزت أصلاً في هذه المحادثة على `/security-review` — لا علاقة لها بـ`/code-review`، الأولى أمنية بحتة عبر GitHub Action، والثانية مراجعة جودة كود عامة تفاعلية.)

---

## 4️⃣ Refactor — لا توجد إضافة رسمية باسم "refactor" بهذا التحديد

نتيجة بحث فعلي: لا توجد إضافة/مهارة معروفة بنفس وزن `frontend-design` أو `Superpowers` مخصصة حصرًا لـ"إعادة الهيكلة". هذا ليس نقصًا في البحث — ببساطة "refactor" في نظام Claude Code البيئي يُعامَل كقدرة تحرير كود أصلية (native)، تُنضبط بأدوات أخرى بدل أن تكون إضافة قائمة بذاتها.

**البديل العملي المبني على ما هو مثبّت عندك بالفعل**: انضباط Superpowers (الذي أقررت تفعيله سابقًا لـ"المرحلة 3") يوفر بالضبط الانضباط المطلوب لإعادة الهيكلة الآمنة: `systematic-debugging` (فهم السبب قبل التعديل) + `test-driven-development` (اختبار يثبت السلوك قبل وبعد) + `verification-before-completion`. أي refactor على مشروع بلا اختبارات (وهذه حالة "سِين" حاليًا) بدون هذا الانضباط = مخاطرة حقيقية بكسر شيء يعمل بصمت.

### أول هدف refactor فعلي مقترح (وليس افتراضيًا — من تدقيق سابق فعلي)
بعد اكتمال "المرحلة 3" من `seen-activate-verticals-task.md` (الكتابة المزدوجة `status`+`status_key`) والتأكد من ثباتها في الإنتاج لفترة كافية: نفّذ عملية refactor منفصلة لاحقة لحذف القراءة من الأعمدة القديمة (enum) في نقاط القراءة المزدوجة، **فقط** بعد أن تثبت الاختبارات (من المرحلة 3) أن القيم الجديدة مطابقة تمامًا للقديمة على كل المستأجرين الحاليين. لا تبدأ هذا قبل ذلك — هذا تسلسل، وليس عملاً متوازيًا.

---

## ✅ معايير القبول (Definition of Done)

- [ ] `frontend-design` مثبّتة، واستخدمت فعليًا على شاشة اختيار النشاط التجاري (تصميم واحد على الأقل تم بناؤه بمنهجيتها الخمس-خطوات)
- [ ] `qa-skills` مثبّتة، وسيناريوهات Playwright مولَّدة فعليًا لدورة حياة الطلب على الأنشطة الخمسة + تسجيل الدخول للأدوار الثلاثة، وتعمل فعليًا (`playwright test` ينجح)
- [ ] `/code-review high` نُفّذ فعليًا على آخر مجموعة تغييرات، والملاحظات 🔴 عولجت أو وُثِّق سبب تأجيلها
- [ ] لم يُنفَّذ أي refactor على الأعمدة القديمة قبل ثبات المرحلة 3 في الإنتاج
- [ ] كل استخدام موثَّق بنتيجته الفعلية (نجح/فشل) لا افتراضًا
